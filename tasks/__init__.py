"""Task contracts.

Import concrete task modules explicitly.  The package intentionally performs
no eager imports so active BlueStacks operators cannot acquire dormant or
future-porting profiles through package initialization.
"""

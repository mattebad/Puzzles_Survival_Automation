# Stage 11 final reconciliation r2

## Control
- Task: `stage-11-final-reconciliation-r2`.
- Supersedes r1 without altering its history. Refreeze reason: final governance validation proved that the schema-3 validator/workflow guard, active task contract, and indexing exclusions are stale relative to the current durable handoff.
- Parent: `gpt-5.6-sol`, sole implementation and integration owner under the continuing Solo route.
- Entry HEAD: `f2e929b5c6e8448c880db574284adcd5099a474f`; uncommitted r1-scoped selector hardening is retained.
- User authorization: explicit on 2026-08-26 through Stage 11, convergence completion, and the merge boundary.

## Reconciled Stage 10 dispositions
- Phases 1-3 accepted; Phase 1 used zero input, Phase 2 used four navigation inputs, and Phase 3 used five navigation inputs plus one zero-cost Praise.
- Phases 4-5 closed `blocked_evidence_required` after one zero-input product-state canary each; no repeats.
- Phase 6 closed `blocked_evidence_required` without a new live attempt because 37 retained dispatch-bearing artifacts exceed the hard one-canary limit.

## Frozen architecture
1. Keep the checked-in disabled production registry as sole production admission authority: zero registered and zero scheduler-eligible entries.
2. Require every production selection handler to receive an explicit exact typed snapshot; `_FIXED_BINDINGS` remains a closed validator, not active authority.
3. Reconcile schema-3 governance around full Git SHA bindings, current lifecycle tokens, bounded generic counters, and completed-stage relations. Do not weaken disabled-registry, absent-lease, clear-unresolved, or protected-evidence invariants.
4. Add the exact Stage 11 backlog contract and merge-boundary successor; mark superseded Stage 10 prose/aliases as historical and non-authorizing.
5. Restore all intended raw/local evidence indexing exclusions while keeping lightweight authoritative manifests indexable.
6. Update workflow-guard binding semantics to accept a full committed SHA ancestor/current handoff binding rather than the obsolete literal token, while preserving prompt, product-precondition, failure-class, time, and counter gates.
7. No runtime, scheduler, production registration, gameplay input, PvP/player attack, premium, or real-money action is permitted.

## Writable paths
- `automation_service/handlers.py`.
- Exact handler/registry/service/scheduler constructor-authority tests.
- `CURRENT_HANDOFF.md`.
- `BACKLOG.md`.
- `docs/runtime-reliability-convergence-status.md`.
- `scripts/validate_governance.py`.
- `.cursor/hooks/pns_agent_workflow_guard.py`.
- `.cursorindexingignore`.
- Exact governance/workflow tests.
- r1 and this r2 manifest.

## Acceptance
- Selector constructors reject missing/non-typed/cross-flow snapshots; service composes registered handlers only from the exact file entry.
- Registry has 24 entries, zero registered, zero scheduler eligible; offline pulse selects nothing with zero transport.
- Governance validator accepts the actual schema-3 handoff, full SHA ancestry, Stage 11 task/successor, lifecycle state, and indexing policy.
- Workflow guard accepts only supported full-SHA handoff binding semantics and otherwise remains fail-closed.
- Stage 10 focused adapters, DevelopmentSession, authority consistency, gameplay contracts, orchestrator, governance, and workflow checks pass or retain an explicit unrelated historical disposition.
- Stage 11 closes completed with no active manifest/runtime/lease/agent/registration/scheduler/unresolved action and an exact authorized merge-boundary successor.
- Merge occurs only after final review and validation, without force, history rewriting, hook bypass, or protected-evidence staging.

## Runtime and mutation budget
- Runtime sessions and gameplay inputs: zero.
- Production registrations/scheduler selections: zero.
- Protected evidence mutations: zero.
- Identical retries: zero.

# Stage 10 phase 6 Troop Training disposition r1

## Control
- Task: `stage-10-phase-6-troop-training-r1`.
- Parent: `gpt-5.6-sol`, sole implementation, integration, and live-runtime owner under the continuing Solo route.
- Entry HEAD: `6010cab` with Stage 10 Phase 5 truthfully closed and production registration disabled.
- User authorization: explicit on 2026-08-26 through Phase 6, Stage 11, convergence completion, and the merge boundary.

## Candidate and hard precondition
- Exact flow: `TROOP-TRAINING-END-TO-END-CONSOLIDATION`.
- Exact product: `troop_training-v1`, profile `pns-bluestacks-5-p64-800x1280-v1`.
- Exact route authority permits at most one dispatch-bearing canary and rejects admission when prior native artifacts contain a dispatch-bearing run.
- Pre-freeze artifact audit found 37 dispatch-bearing retained run artifacts under `.local-captures/flow-delivery/TROOP-TRAINING-END-TO-END-CONSOLIDATION`, while `MAX_DISPATCH_BEARING_CANARY_RUNS` is 1.

## Frozen architecture
1. Preserve the existing Troop Training controller, product authority, configuration, verifier, and hard maximum-live-canary fence.
2. Do not delete, relabel, ignore, or archive prior dispatch-bearing evidence to manufacture eligibility.
3. A future scheduler promotion would require a fresh queue-generation projection, exact free-slot generation identity, atomic registration consumption before observation, and one immutable typed registration snapshot.
4. Success would require exact configured tier/quantity, known resources, one normal timed Train successor with queue label/tier/quantity/positive timer, no Train Now/premium/speedup/Cash Mall/forbidden resource boxes, exact total input accounting, and canonical Home.
5. Because the existing canary fence is already exhausted, do not register the flow, do not select a scheduler occurrence, do not acquire runtime ownership, and do not issue a live input in this phase.

## Writable paths
- This manifest.
- `CURRENT_HANDOFF.md`.
- `docs/runtime-reliability-convergence-status.md`.
- No production implementation, registry, queue, controller, verifier, or runtime path is writable under this disposition.

## Acceptance
- Re-run the checked-in `_prior_dispatch_bearing_runs` audit without mutation and retain its count.
- Confirm the checked-in production registry remains disabled for Troop Training and no other flow is registered.
- Confirm no runtime process or DevelopmentSession is created for Phase 6.
- Close as `blocked_evidence_required` with failure class `prior_canary_budget_exhausted`; do not repeat or relabel historical dispatches.
- Stage 11 may proceed after this serial disposition.

## Live budget
- New live canaries: zero.
- New native inputs: zero.
- New Train actions: zero.
- Premium, speedup, Train Now, Cash Mall, resource-box, PvP/player attack, and real-money inputs: zero.
- Identical retries: zero.

# Stage 10 phase 3 Nova Praise promotion r1

## Control
- Task: `stage-10-phase-3-nova-praise-r1`.
- Parent: `gpt-5.6-sol-medium`, sole control-plane and live-runtime owner.
- Mutable role: one mapped `gpt-5.6-luna-xhigh` bounded implementation.
- Independent role: one mapped `gpt-5.6-terra-high` read-only review and at most one finding-only recheck.
- User continuation: explicit on 2026-08-25 through Phase 6; Phase 7 combat remains unauthorized.
- Entry HEAD: `695850e1b6e14c910ee37f8c285c834ebba78523`, synchronized with upstream and clean.
- Phase 2 prerequisite: accepted and published; all registration disabled and runtime ownership released.

## Candidate and product precondition
- Exact flow: `NOVA-PRAISE-SUPERVISED-ONE-FREE-PULSE`.
- Exact product: `nova_praise-v1`, digest `959fe8201ce0250dcab494dc65f930cf52c753b1ac5833d22bcb3a1abea2b2ae`, profile `pns-bluestacks-5-p64-800x1280-v1`.
- Product authority permits exactly one eligible zero-cost Praise, requires attempts `X -> X-1`, a 300-second policy cooldown successor, confirmed action persistence, and canonical Home. Dispatch is not success.
- Stage 8 accepted the same flow with continuous current native proof, same-reset duplicate denial after restart, and next-reset eligibility. The retained 2026-08-24 occurrence must not be reused or relabeled.
- Before dispatch, bind the current static-UTC reset identity to `game-day-2026-08-25`, fresh zero-input native evidence, `primary-account`, `primary-server`, and the private local BlueStacks runtime.

## Frozen architecture
1. The checked-in registry remains the sole registration authority, defaults every flow disabled, and permits at most one registered flow.
2. The exact allowlist contains only the already accepted World binding and this exact Nova binding. No arbitrary handler, product, profile, or mode is accepted.
3. Nova scheduler selection is zero-transport and terminally hands the parent one current-reset occurrence. It cannot bind a target, acquire runtime ownership, create a DevelopmentSession, invoke the route, or issue input.
4. The scheduler occurrence cadence for this bounded promotion is once per reset, preserving the accepted same-reset duplicate fence. The underlying product still verifies its exact Praise cooldown; no generic cooldown scheduling authority is added.
5. The live Nova route atomically validates and consumes the exact current registration before DevelopmentSession creation or observation. Every success, defer, block, unknown, or exception leaves registration disabled.
6. The immutable registered dispatch snapshot is retained in result and causal trace. Verification rehydrates only the exact Nova snapshot and rejects missing, forged, partial, mismatched, or contradictory evidence after consumption.
7. Existing Nova current-frame target, one-Praise ceiling, zero-cost guard, action journal, unknown-effect reconciliation, no-identical-retry guard, and Home terminal remain unchanged.
8. An unknown Praise effect enters reconciliation and permanently closes this phase dispatch budget. No redispatch occurs.

## Writable paths
- `automation_service/registry.py`
- `automation_service/handlers.py`
- `automation_service/service.py`
- `scripts/pnsctl.py`
- `scripts/flow_delivery_nova_praise_bluestacks.py`
- `tasks/flow_delivery_disabled_production_registry.json`
- Exact affected tests under `tests/test_automation_service_*.py`, `tests/test_pnsctl_scheduler_pulse.py`, `tests/test_development_session.py`, and `tests/test_flow_delivery_nova_praise_bluestacks.py`.
- Parent closure only: `CURRENT_HANDOFF.md`, `docs/runtime-reliability-convergence-status.md`, and this manifest.

## Acceptance
- Strict registry tests permit zero or one exact World/Nova registration and reject multiple, malformed, or cross-bound bindings.
- A healthy offline pulse selects only Nova with `transport_count=0`; duplicate and reopened-store pulses select nothing.
- Product, registration, owner, clock, reset, unresolved-occurrence, and profile mismatches select nothing.
- Live full-route registration is consumed atomically before runtime observation; a second invocation rejects before ownership or runner.
- Nova result and causal trace retain the exact immutable registered snapshot; forged/mismatched snapshots fail verification.
- Existing Stage 9 persistence, rollback, reset, unresolved, and abandoned-repeat regressions remain passing.
- Existing Nova safety, continuous-session, effect, restart, and no-identical-retry tests remain passing.
- Terra returns `ADMIT_LIVE_CANARY`; Sol accepts integration before runtime.
- One fresh zero-input observation establishes current native Home and reset evidence, then at most one live current-reset Nova occurrence may run.
- Live success requires one Praise, exact attempts decrement, cooldown successor, confirmed journal, persisted occurrence/action, continuous proof, and canonical Home.
- Final registry is disabled on every terminal path.

## Live budget
- One Nova current-reset canary.
- Maximum eight total inputs: existing bounded navigation plus at most one zero-cost Praise.
- One Praise transport maximum.
- Zero paid/premium fallback, Daily claim, resource, purchase, maintenance, AP/stamina, node, march, formation, combat, or real-money input.
- Identical retries: zero.
- Phase 4 remains separately unadmitted until Phase 3 is accepted or truthfully dispositioned. Phase 7 combat remains unauthorized.

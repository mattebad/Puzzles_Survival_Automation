# DQ-FLOW-RUINS-CHALLENGE-BLUESTACKS

Commit: retain the bounded task-local BlueStacks route, contracts, tests, and concise decisive
evidence together; production registration and scheduler eligibility remain disabled.

Repository authority: `BACKLOG.md` owns this task, the execution matrix remains authoritative for
production registration, and the local evidence manifest owns retained BlueStacks evidence.

Scope: separate Ruins Challenge Daily route only. Route: Home/Base → Ruins → fresh current-day
rows → detail → Attack → zero-cost NPC Dispatch → explicit result → Ruins list → Home/Base. Source:
fresh native 800x1280 frames. Target: exact recognized challenge or chest control. Successor: explicit
result/progress or exact chest disappearance/state change. Bind reset identity and frame hash.

Policy: only currently available, non-premium, non-paid, non-ticketed, zero-cost rows are eligible.
Reject locked, unavailable, expired, wrong-day, unknown, Exchange, Mall, purchase, ticket, and points-
spending controls. Every consequential input requires fresh target binding and exact postcondition
evidence. One required Daily challenge is sufficient; an optional second needs a distinct
positively recognized row. Claim remains separate from both challenge completion and Daily Claim.

Postcondition: explicit WIN with increased progress, or explicit LOSE reconciled without retry; chest
Claim must produce a positive chest state change. Recovery: stop on stale, overlay, unknown result,
missing close, or unresolved action. Persistence tracks each challenge and chest independently.

Tests: `tests/test_ruins_challenge.py` covers identity, current-day filtering, one/two available rows,
locked/wrong-day rejection, native vision, dispatch/result guards, progress, duplicate prevention,
chest separation, Daily completion, and disabled registration/scheduler. Bliss and GnBots material do
not authorize this local workflow. Daily Claim is not performed. Production promotion remains off.

# Stage 10 phase 3 Nova Praise promotion r3

## Control
- Task: `stage-10-phase-3-nova-praise-r3`.
- Parent: `gpt-5.6-sol-medium`, sole control-plane and live-runtime owner.
- Mutable role: one mapped `gpt-5.6-luna-xhigh` bounded correction.
- Independent role: one mapped `gpt-5.6-terra-high` read-only review and at most one finding-only recheck.
- User continuation: explicit after the r2 `diminishing_returns` stop.
- Entry HEAD: `6bcf284fe244fb9266ad677c58f0da5df8522e58`, synchronized with upstream and clean.
- Phase 7 combat remains unauthorized.

## Frozen correction
1. Preserve the r2 exact registry allowlist, zero-transport scheduler, once-per-reset occurrence, atomic pre-runtime registration consumption, typed snapshot, terminal disable, and Nova safety contracts.
2. In `_verify_nova_supervised_one_free_pulse_session`, after existing file/type/symlink/snapshot/scheduler checks, require the complete parsed `causal-trace.json` object to equal both complete embedded causal traces from `result.json` and `flow-delivery-result.json`.
3. Reject any missing, extra, or changed non-registration trace field. Do not normalize away differences or compare only a projection.
4. Preserve genuine verifier acceptance and all forged/missing/partial registration evidence rejection.
5. No registration enablement, scheduler pulse, DevelopmentSession, runtime ownership, observation, or gameplay input before parent integration acceptance.

## Writable paths
- `scripts/pnsctl.py`
- `tests/test_development_session.py`
- Parent closure only: `CURRENT_HANDOFF.md`, `docs/runtime-reliability-convergence-status.md`, and this manifest.

## Acceptance
- One focused regression mutates a non-registration field in on-disk `causal-trace.json` and the verifier rejects it.
- Missing/forged/mismatched trace and snapshot regressions remain passing.
- Genuine persisted Phase 3 artifacts verify.
- Exact DevelopmentSession tests and the affected Phase 3 scheduler/Nova suite pass.
- Terra returns `ADMIT_LIVE_CANARY`; Sol accepts integration before any runtime action.
- Registry remains disabled until a separately published reviewed candidate deliberately enables the one exact Nova canary.
- Live budget remains unused: one current-reset canary, maximum eight total inputs, maximum one zero-cost Praise, zero forbidden actions, zero identical retries.
- Phases 4-6 remain unadmitted until Phase 3 is accepted or truthfully blocked. Phase 7 remains unauthorized.

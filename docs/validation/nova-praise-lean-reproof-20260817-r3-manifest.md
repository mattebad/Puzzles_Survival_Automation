# Nova Praise lean reproof — frozen revision r3

This revision supersedes the immediate-before safety design in r2. All r1 input,
registration, scheduler, reset-guard, return, and live-iteration limits remain.

## Frozen stage control
- Task / flow: `nova-praise` / `NOVA-PRAISE-SUPERVISED-ONE-FREE-PULSE`
- Revision ID: `nova-praise-lean-reproof-r3`
- Stage type: `safety_boundary_redesign`
- Failure class: `core_contract`
- User continuation: `2026-08-18T03:47Z`
- `control_plane_owner`: `gpt-5.6-sol-medium`
- `bounded_implementer`: parent takeover after interrupted `gpt-5.6-luna-xhigh`
- `independent_tester`: `gpt-5.6-terra-high`

## Frozen repair decision
- The full-OCR proposal remains an entry gate only.
- The immediate native frame must be independently recognized through the same
  staged Nova recognizer. Its frame digest, attempts, Praise identity/ROI,
  enabled state, cooldown state, and narrow semantic diagnostics must all bind
  to that current frame.
- The centralized paid/confirmation marker check runs on the immediate
  recognition. No proposal semantics or aggregate image-change threshold may
  substitute for current-frame semantic evidence.
- Radial and measured-Home recognition remain zero-OCR. Current Nova recognition
  uses only the named Nova ROIs; no broad/full-frame OCR is permitted.

## Writable repair paths
- `tasks/nova_praise_vision.py`
- `scripts/nova_praise_centralized.py`
- `tests/test_nova_navigation_canary.py`
- `tests/test_nova_praise_centralized_boundary.py`

## Acceptance
- A newly present compact paid/confirmation marker on the immediate frame blocks
  before journal preparation or transport.
- Changed attempts, disabled/moved Praise, cooldown, stale or non-native frames,
  missing current recognition, and digest mismatch fail closed.
- The retained immediate-before frame remains recognized in a measured duration
  below the two-second dispatch-age ceiling.
- Existing r1/r2 focused checks pass apart from separately classified baseline
  workflow-state failures.
- Independent tester recheck resolves the prior P1 before live admission.

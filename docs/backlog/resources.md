# Resource recovery backlog

- Planning-only records for the resource/effect and resource-policy slice. Every ticket is inactive: these records authorize no transport, scheduler registration, production enablement, native observation, evidence mutation, or product action. `ResourceEffectAuthority` remains authoritative for every effectful route until REC-R01's parity package is accepted. An `UNKNOWN` effect is reconciled or blocked; it is never automatically retried. Shared implementation files are merged serially under the mutex named by each ticket; shared family static bindings and SQLite-disabled state are facts to preserve, not separate mutable registration gates.

### REC-R01 — Preserve ResourceEffectAuthority parity at the canonical action boundary
- Task ID: `REC-R01`
- Type: Bug
- Priority: P0
- Status: WAITING_DEPENDENCIES
- Objective: Preserve observable occurrence, quantity, reservation, effect-confirmation, changed-hypothesis, and crash/UNKNOWN safeguards while resource routes move from retained delivery adapters to the canonical action lifecycle; no authority may be retired on the strength of a schema-only match.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`
- Related work: `DAILY-RESOURCE-ITEM-BLUESTACKS-INTEGRATION` (reuse the one-item effect facts, not its completed label as canonical readiness); `RUNTIME-RELIABILITY-MERGE-BOUNDARY` (retain the zero-authority end state). No separate historical parity task exists; the authority itself and governance plan are retained.
- Evidence: `governance-simplification-plan.md` requires `ResourceEffectAuthority` to remain authoritative until parity covers occurrence, quantity, reservation, unknown effects, and changed-hypothesis retries; `safe_action_core/resource_effect_authority.py` exposes `create_resource_occurrence`, `claim_resource_attempt`, `prepare_resource_effect_action`, `record_resource_transport_intent`, `dispatch_prepared_resource_item_use`, and observe-only reconciliation; `tests/test_resource_effect_authority_store.py` and `tests/test_resource_effect_authority_history.py` exercise the retained stores. Prior audit evidence was isolated fake/temp state proof, not native acceptance.
- Scope: `safe_action_core/resource_effect_authority.py`; `safe_action_core/executor.py`; `automation_service/actions.py`; `tests/test_resource_effect_authority_store.py`; `tests/test_resource_effect_authority_history.py`
- Changes: Define one typed adapter from canonical action intent to the existing authority context; carry reset identity, occurrence key, exact quantity, reservation generation, current-frame provenance, and policy revision through reservation and dispatch; prove parity before any cutover. A crash after `DISPATCHING` becomes `UNKNOWN`; reconciliation may resolve it or block, never repeat it. A `NO_EFFECT` retry must create a new action with a materially changed hypothesis. Merge mutex: serialize with every R02–R19 effectful route because all touch the shared action/effect boundary.
- Non-goals: Do not delete or bypass `ResourceEffectAuthority`; do not redesign SQLite schema, add a second executor, import protected evidence, alter route policy, or enable any flow.
- Acceptance: 1. Same reset/route occurrence cannot produce two claimed effects after restart or concurrent claim. 2. Quantity, reserve, and per-route budget mismatches block before dispatch. 3. Crash before reservation, after reservation, during dispatch, and before reconciliation have explicit terminal states. 4. `UNKNOWN` never causes an automatic repeat and cannot be cleared by a changed hypothesis alone; it must first be semantically reconciled to `NO_EFFECT`. Any other explicit terminal outcome closes the occurrence. Only after `NO_EFFECT` may a new action proceed, and that retry requires explicit changed-hypothesis authorization. 5. Canonical and retained authorities agree on occurrence, quantity, reservation, effect outcome, and reset ownership across crash/restart probes. 6. The old authority remains authoritative until every parity invariant is observed; no route is enabled by this ticket.
- Verification: Existing `python -m unittest tests.test_resource_effect_authority_store tests.test_resource_effect_authority_history tests.test_safe_action_core tests.test_pre_dispatch_freshness` is prescribed after implementation; no command is run by this planning ticket.
- Native gate: None for the offline parity implementation; each later consequential route still requires its own current native route record through `REC-O10`, and old native/session records remain historical rather than canonical acceptance.
- Integration owner: Canonical service owner must update `automation_service/actions.py:ActionExecutor.execute`, `safe_action_core/executor.py:SafeActionExecutor.execute`, `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, and `automation_service/registry.py:build_canonical_handler` in one disabled-gate cutover for this adapter and its affected binding; do not require simultaneous migration of every resource route. Shared family static bindings and SQLite-disabled state remain preserved facts, not separate registration mutations; all other routes stay inactive.
- PR boundary: `recovery/rec-r01` — one parity adapter and focused proof package.
- Rollback: Disable the affected SQLite flow/global gate and keep the retained authority; never restore a competing legacy dispatch path.
- Runtime authorization: none.
- Completion: Parity proof and atomic cutover proof are deliverables; merged code does not imply native acceptance or route enablement.

### REC-R02 — Use exactly one 1K Food item without Daily inheritance
- Task ID: `REC-R02`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Cut over the retained `use_resource_item` route to exactly one positively bound 1K Food use per permitted occurrence, with exact resource delta and canonical Home reconciliation, without treating the effect as selected-Daily credit.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-R01`
- Related work: `DAILY-RESOURCE-ITEM-BLUESTACKS-INTEGRATION` (retain the supervised one-use evidence and route semantics; completed queue status is not canonical readiness); `DQ-FLOW-RESOURCE-BOOST` (unrelated boost policy, do not merge). Daily Claim remains a separate owner in the legacy Daily records.
- Evidence: `scripts/daily_resource_item_bluestacks.py` and `tests/test_daily_resource_item_bluestacks.py` retain the 1K Food route shape; `safe_action_core/resource_effect_authority.py` fixes `RESOURCE_TARGET_VARIANT="1k_food"`, `RESOURCE_QUANTITY=1`, and resource occurrence/effect lifecycle; BACKLOG historical evidence records one supervised direct use and observed delta/Home, but no production authority.
- Scope: `scripts/daily_resource_item_bluestacks.py`; `tests/test_daily_resource_item_bluestacks.py`; `safe_action_core/resource_effect_authority.py`; `tests/test_resource_effect_authority_store.py`
- Changes: Reuse the exact 1K Food target and retained authority; require current item identity, quantity one, known zero-premium policy, fresh before/after balances, and canonical Home successor. Persist one deterministic occurrence and refuse a second use in that reset. Keep selected-Daily catalog/Claim ownership outside this route; any future Daily mapping must be an explicit D-ticket integration, not inferred from the resource delta. Merge mutex: serialize with R01 and all other resource/effect callers.
- Non-goals: No premium item, refill, arbitrary resource pack, quantity editing, Daily Claim, Supply Depot, or automatic retry after an unknown delta.
- Acceptance: 1. Only `1k_food` quantity one can reach reservation. 2. Missing/ambiguous item, cost, balance, frame, successor, or delta blocks before or after input. 3. An observed effect is reconciled exactly once and a second same-occurrence attempt is denied. 4. The retained historical one-use session is referenced as evidence only; no status or registration is promoted by it. 5. The route returns canonical Home (or blocks without follow-up input) and selected-Daily progress remains unchanged unless separately observed by its owner.
- Verification: Existing `python -m unittest tests.test_daily_resource_item_bluestacks tests.test_resource_effect_authority_store tests.test_resource_effect_authority_history` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current route-specific 1K Food scenario with exact before/after delta, one dispatch, and Home proof; the 2026-08-19 supervised record is historical and cannot satisfy that gate alone.
- Integration owner: Update `automation_service/registry.py:CANONICAL_FLOW_REGISTRY` and `automation_service/handlers.py` for one disabled-by-default resource runner, plus `scripts/flow_delivery_daily_resource_item_bluestacks.py:register` and the exact `tasks/gameplay_flow_contracts/DAILY-RESOURCE-ITEM-BLUESTACKS-INTEGRATION.json` registration hunk; remove/hard-disable any competing live binding atomically.
- PR boundary: `recovery/rec-r02` — one 1K Food resource-use cutover.
- Rollback: Disable the resource flow and retain `ResourceEffectAuthority` reservations/blocks; do not replay or restore a legacy bypass.
- Runtime authorization: none.
- Completion: Offline contract and cutover can merge independently; native acceptance, Daily credit, registration, and scheduling remain separate.

### REC-R03 — Gear one-star single-material enhancement
- Task ID: `REC-R03`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Route one selected equipped Gear `Enhance` action through the shared family contract using exactly one positively identified one-star material, preserving exact target, cost, material, successor, and effect accounting.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-R01`
- Related work: `DQ-FLOW-ENHANCE-GEAR` (retain exact Gear/one-star/quantity-one semantics and offline coverage; its Passed label is not canonical readiness); `ENHANCEMENT-FAMILY-BLUESTACKS-INTEGRATION` (reuse the family runner and historical per-category reservations; retain the documented family mutex).
- Evidence: `tasks/enhancement.py` defines `SUPPORTED_VARIANTS`, `enhancement_authorizeable`, transaction specs, and same-category postcondition checks; `tasks/daily_enhancement.py` preserves selected-row/family separation; `scripts/enhancement_bluestacks.py` is the retained category runner; `tests/test_enhancement.py` and `tests/test_daily_enhancement.py` cover exact material and cost guards.
- Scope: `tasks/enhancement.py`; `tasks/daily_enhancement.py`; `scripts/enhancement_bluestacks.py`; `tests/test_enhancement.py`; `tests/test_daily_enhancement.py`
- Changes: Keep Gear as a distinct variant identity in the family controller; bind current equipped target and one-star material inside the same panel/frame; require exact observed cost and quantity one; send one Enhance/Confirm and reconcile level/material/result delta through R01. Unknown cost/material/target/successor is a block. Merge mutex: serial with REC-R04 and REC-R05 because they share `tasks/enhancement.py`, `tasks/daily_enhancement.py`, and the family script; do not make one variant's semantics depend on another.
- Non-goals: No Chip/Module dispatch, Auto Select, higher-star material, Promote, Modify, Replace, Unequip, Inherit, premium currency, or Claim ownership.
- Acceptance: 1. Gear is the only accepted family identity. 2. Quantity is exactly one and material star is exactly one; unknown cost or resource blocks. 3. Source/target ROIs and successor are same-category and current-frame bound. 4. A changed level/material/result is required after one dispatch; unknown effect is terminal and never automatically retried. 5. Selected-Daily reconciliation, if applicable, remains external to the enhancement route.
- Verification: Existing `python -m unittest tests.test_enhancement tests.test_daily_enhancement tests.test_flow_delivery_enhancement` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current Gear scenario with native before/after, exact one-star material, one dispatch, effect delta, and Home; retained local/family sessions are not current acceptance.
- Integration owner: Update `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, `automation_service/handlers.py`, `scripts/flow_delivery_enhancement_bluestacks.py:register`, and the Gear row in `tasks/gameplay_flow_contracts/ENHANCEMENT-FAMILY-BLUESTACKS-INTEGRATION.json` in one disabled-gate cutover; retain one shared family owner.
- PR boundary: `recovery/rec-r03` — Gear-only enhancement variant.
- Rollback: Disable Gear flow and preserve family reservations/effect authority; never enable the old Gear dispatch alongside canonical actions.
- Runtime authorization: none.
- Completion: Gear offline contract and its exclusive wiring may merge; native/evidence/enablement readiness remain separate.

### REC-R04 — Chip one-star single-material enhancement
- Task ID: `REC-R04`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Route one selected equipped Chip `Enhance` action through the shared family contract using exactly one one-star material and no unapproved resource or premium behavior.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-R01`
- Related work: `DQ-FLOW-ENHANCE-CHIP` (retain Chip-specific identity and focused tests; historical Passed is not canonical readiness); `ENHANCEMENT-FAMILY-BLUESTACKS-INTEGRATION` (reuse shared family implementation and validator, with a serial variant merge).
- Evidence: `tasks/enhancement.py`'s `SUPPORTED_VARIANTS`, `enhancement_authorizeable`, and postcondition functions distinguish Chip from Gear/Module; `tasks/daily_enhancement.py` has the selected-row adapter; `tests/test_daily_enhancement_chip.py` and `tests/test_enhance_chip.py` cover Chip family separation and one-star guards; the family script's category accounting is retained historical implementation.
- Scope: `tasks/enhancement.py`; `tasks/daily_enhancement.py`; `scripts/enhancement_bluestacks.py`; `tests/test_enhance_chip.py`; `tests/test_daily_enhancement_chip.py`
- Changes: Add only Chip's exact variant binding to the canonical family entry; require current equipped Chip, one-star material, quantity one, known exact cost, same-panel target, and positive same-category result. Preserve R01 reservation/effect lifecycle and fail closed on any cross-variant or changed-frame observation. Merge mutex: serialize with REC-R03 and REC-R05 over the shared family module/script; variant behavior remains independently testable.
- Non-goals: No Gear/Module dispatch, Auto Select, higher-star material, Promote/Modify/Replace/Unequip/Inherit, premium, purchase, or Daily Claim.
- Acceptance: 1. Only Chip identity and its current-frame target can authorize. 2. Exactly one one-star material is selected; unknown resource or cost blocks. 3. No same-frame duplicate or cross-category target is accepted. 4. Positive Chip successor/effect is required; `UNKNOWN` is retained for reconciliation and never retried automatically. 5. Existing selected-Daily owner remains separate.
- Verification: Existing `python -m unittest tests.test_enhancement tests.test_enhance_chip tests.test_daily_enhancement_chip tests.test_flow_delivery_enhancement` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current Chip scenario and independently verify exact target, material, effect, and Home; historical family evidence is reusable context only.
- Integration owner: Update the same canonical family registry/handler and `scripts/flow_delivery_enhancement_bluestacks.py:register` with a Chip-specific row in the shared contract, atomically and while the global gate is disabled.
- PR boundary: `recovery/rec-r04` — Chip-only enhancement variant.
- Rollback: Disable Chip row and preserve R01/family authority; no legacy concurrent dispatch.
- Runtime authorization: none.
- Completion: Chip route code and focused proof are one variant PR; registration/scheduling/native readiness remain separate.

### REC-R05 — Module one-star single-material enhancement
- Task ID: `REC-R05`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Route one selected equipped Module `Enhance` action through the shared family contract using exactly one one-star material, with Module-specific target and effect reconciliation.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-R01`
- Related work: `DQ-FLOW-ENHANCE-MODULE` (retain Module identity and focused tests; historical Passed is not canonical readiness); `ENHANCEMENT-FAMILY-BLUESTACKS-INTEGRATION` (reuse family runner and serial category reservations).
- Evidence: `tasks/enhancement.py` defines Module in `SUPPORTED_VARIANTS`, exact family authorization, transaction specs, and same-category postconditions; `tasks/daily_enhancement.py` and `tests/test_daily_enhancement_module.py` preserve Module/Chip/Gear boundaries; `tests/test_enhance_module.py` covers shared material guards; `scripts/enhancement_bluestacks.py` retains the native family implementation.
- Scope: `tasks/enhancement.py`; `tasks/daily_enhancement.py`; `scripts/enhancement_bluestacks.py`; `tests/test_enhance_module.py`; `tests/test_daily_enhancement_module.py`
- Changes: Add only Module's exact variant binding to the canonical family entry; require current equipped Module, one-star material, quantity one, observed exact cost, same-panel target, and positive same-category successor/effect through R01. A changed frame, unknown material/cost, or unexpected result blocks. Merge mutex: serialize with REC-R03 and REC-R04 over shared family files and shared registry hunks.
- Non-goals: No Gear/Chip dispatch, Auto Select, higher-star material, Promote/Modify/Replace/Unequip/Inherit, premium, purchase, or Claim.
- Acceptance: 1. Module identity must be positively current-frame bound. 2. Exactly one one-star material and known non-premium cost are required. 3. One dispatch maximum per occurrence; duplicate target or stale frame is refused. 4. Positive Module effect/successor is required; `UNKNOWN` never auto-retries. 5. Daily progress/Claim remains owned by its separate canonical route.
- Verification: Existing `python -m unittest tests.test_enhancement tests.test_enhance_module tests.test_daily_enhancement_module tests.test_flow_delivery_enhancement` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current Module scenario with exact material/effect and Home proof; old family labels and captures are not canonical acceptance.
- Integration owner: Update the canonical family registry/handler, `scripts/flow_delivery_enhancement_bluestacks.py:register`, and Module contract row atomically under the disabled gate; do not leave an old family runner live.
- PR boundary: `recovery/rec-r05` — Module-only enhancement variant.
- Rollback: Disable Module row and retain R01/family authority; never restore a parallel legacy dispatcher.
- Runtime authorization: none.
- Completion: Module route and focused proof are complete only for offline/code merge; native acceptance and enablement remain separate.

### REC-R06 — Fighter T8 current-maximum training with approved boxes
- Task ID: `REC-R06`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Train Fighter at T8 for the current numeric maximum quantity under the continuous policy, permitting resource boxes only where the exact approved continuation is positively identified.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `TROOP-TRAINING-END-TO-END-CONSOLIDATION` (retain exact Fighter T8/current-max native observations and Home terminal, not its completed label as canonical readiness); `DQ-FLOW-TRAINING` (retain four-way disabled contract as historical policy); `RECRUITMENT-FREE-ATTEMPT-MAINTENANCE` (unrelated maintenance; no shared Daily ownership).
- Evidence: `tasks/troop_training.py` defines `TrainingConfig`, `TroopTrainingConfig`, resource readings, quantity/tier guards, and `TrainingController`; `tasks/troop_training_runtime.py` owns state transitions and unresolved action handling; `scripts/troop_training_bluestacks.py` and `scripts/flow_delivery_troop_training_bluestacks.py` retain current facility/queue route; `tasks/flow_delivery_product_policy.json` explicitly permits Fighter T8 current maximum, continuous, boxes allowed, with registration disabled.
- Scope: `tasks/troop_training.py`; `tasks/troop_training_runtime.py`; `scripts/troop_training_bluestacks.py`; `tests/test_troop_training.py`; `tests/test_training_disabled.py`
- Changes: Bind Fighter facility and T8 on a fresh frame; calculate the current numeric maximum rather than defaulting to 250; require known exact costs and capacity; permit only an explicit resource-box continuation for Fighter, through R01, with no silent box selection. Persist continuous queue state and return Home after one bounded start/reconciliation. Merge mutex: serialize with REC-R07–REC-R09 over troop controller/runtime and shared registry hunks.
- Non-goals: No Rider/Shooter/Vehicle behavior, Train Now, speedup, premium, arbitrary tier, oversized quantity, or Daily completion inheritance.
- Acceptance: 1. Fighter/T8/current numeric maximum/continuous identity is required; a missing or nonnumeric maximum blocks. 2. Boxes are allowed only for an explicitly recognized Fighter continuation and exact resource policy; unknown resource or cost blocks. 3. Queue/timer/capacity successor is current-frame and exact. 4. One unresolved/crashed start is reconciled or blocked and never auto-retried. 5. No selected-Daily credit is inferred from training completion.
- Verification: Existing `python -m unittest tests.test_troop_training tests.test_training_disabled tests.test_flow_delivery_troop_training` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must assign a current Fighter T8 scenario with numeric maximum, approved-box decision, exact queue/cost, one start, and Home; retained 2026-08-14 evidence is historical reusable context only.
- Integration owner: Update `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, `automation_service/handlers.py`, `scripts/flow_delivery_troop_training_bluestacks.py:register`, and the Fighter row in `tasks/gameplay_flow_contracts/TROOP-TRAINING-END-TO-END-CONSOLIDATION.json` atomically; old four-variant bindings must not dispatch concurrently.
- PR boundary: `recovery/rec-r06` — Fighter training variant.
- Rollback: Disable Fighter row and preserve queue/effect authority; never fall back to unbounded legacy training.
- Runtime authorization: none.
- Completion: Fighter implementation can merge only after shared parity and exact variant cutover; native/evidence/enablement remain independent.

### REC-R07 — Rider T1 once-daily 250 training without boxes
- Task ID: `REC-R07`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Train Rider exactly 250 units at T1 at most once per reset/day under the once-daily policy, with resource boxes structurally prohibited.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `TROOP-TRAINING-END-TO-END-CONSOLIDATION` (retain Rider T1 x250 queue facts and Home recovery as historical evidence); `DQ-FLOW-TRAINING` (retain disabled four-variant contract and exact quantity requirement).
- Evidence: `tasks/troop_training.py` supplies `TrainingConfig`, fixed quantity and tier validation, `TrainingController`, and reset-scoped state; `tasks/troop_training_runtime.py` has bounded start/reconcile phases; `tests/test_troop_training.py` covers queue arithmetic; `tasks/flow_delivery_product_policy.json` requires Rider T1 fixed 250, once_daily, boxes prohibited and Daily inheritance prohibited.
- Scope: `tasks/troop_training.py`; `tasks/troop_training_runtime.py`; `scripts/troop_training_bluestacks.py`; `tests/test_troop_training.py`; `tests/test_training_disabled.py`
- Changes: Bind Rider facility/tab and T1; require exactly 250, known base-resource cost and capacity, and no resource-box continuation branch; enforce one successful start per reset and retain independent cooldown/queue state. Route through R01 for effect reservation and unknown reconciliation. Merge mutex: serial with REC-R06, REC-R08, and REC-R09 over shared training files.
- Non-goals: No Fighter/Shooter/Vehicle, current-max substitution, boxes, Train Now, speedup, premium, or selected-Daily completion.
- Acceptance: 1. Rider/T1/fixed-250/once_daily must match one current frame. 2. Any box prompt, unknown cost/resource, non-empty queue, or quantity other than 250 blocks. 3. A second same-reset start is refused even after restart. 4. Active queue/timer and positive successor are required; `UNKNOWN` is never automatically repeated. 5. Training does not create Daily ownership by itself.
- Verification: Existing `python -m unittest tests.test_troop_training tests.test_training_disabled tests.test_flow_delivery_troop_training` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current Rider T1/250 no-box scenario with one start, exact resource delta, queue successor, and Home; historical consolidation evidence is not current acceptance.
- Integration owner: Update the shared troop registry/handler, Rider contract row in `tasks/gameplay_flow_contracts/TROOP-TRAINING-END-TO-END-CONSOLIDATION.json`, and `scripts/flow_delivery_troop_training_bluestacks.py:register` in one disabled-gate cutover.
- PR boundary: `recovery/rec-r07` — Rider training variant.
- Rollback: Disable Rider row and retain R01/queue state; do not enable a box-capable or legacy fallback.
- Runtime authorization: none.
- Completion: Rider code/contract is complete only for the offline/atomic cutover boundary, not native or scheduler promotion.

### REC-R08 — Shooter T8 once-daily 250 training without boxes
- Task ID: `REC-R08`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Train Shooter exactly 250 units at T8 at most once per reset/day under the once-daily policy, with boxes structurally prohibited.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `TROOP-TRAINING-END-TO-END-CONSOLIDATION` (retain Shooter T8 x250 queue facts and Home recovery as historical evidence); `DQ-FLOW-TRAINING` (retain disabled four-variant quantity/tier contract).
- Evidence: `tasks/troop_training.py` and `tasks/troop_training_runtime.py` provide tier, fixed-quantity, queue, timer, unresolved, and reset seams; `tests/test_troop_training.py` and `tests/test_training_disabled.py` cover four-way boundaries; the product policy record explicitly says Shooter T8 fixed 250, once_daily, no boxes, no Daily inheritance.
- Scope: `tasks/troop_training.py`; `tasks/troop_training_runtime.py`; `scripts/troop_training_bluestacks.py`; `tests/test_troop_training.py`; `tests/test_training_disabled.py`
- Changes: Bind Shooter facility/tab and T8; require fixed 250 and known base-resource cost/capacity; reject any box prompt or alternate tier/quantity; enforce one successful start per reset and R01 effect reservation. Merge mutex: serial with REC-R06, REC-R07, and REC-R09 over the shared troop controller and registration hunks.
- Non-goals: No Fighter/Rider/Vehicle, current maximum, boxes, Train Now, speedups, premium, or Daily claim.
- Acceptance: 1. Shooter/T8/fixed-250/once_daily identity is current-frame bound. 2. Box-capable or unknown-resource paths are denied, not silently converted. 3. Same-reset duplicate, stale target, queue ambiguity, or cost ambiguity blocks. 4. Positive queue/timer successor is required; unknown dispatch never auto-retries. 5. No Daily route inherits this training effect without an explicit owner mapping.
- Verification: Existing `python -m unittest tests.test_troop_training tests.test_training_disabled tests.test_flow_delivery_troop_training` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current Shooter T8/250 no-box scenario and verify one start, exact cost/delta, queue, and Home; prior native consolidation is historical evidence only.
- Integration owner: Update the shared troop registry/handler, Shooter row in `tasks/gameplay_flow_contracts/TROOP-TRAINING-END-TO-END-CONSOLIDATION.json`, and flow-delivery registration in one disabled-gate atomic cutover.
- PR boundary: `recovery/rec-r08` — Shooter training variant.
- Rollback: Disable Shooter row; preserve R01 and no-box prohibition; no legacy concurrent dispatch.
- Runtime authorization: none.
- Completion: Shooter offline and atomic wiring only; native/evidence/scheduling remain separate.

### REC-R09 — Vehicle T1 current-maximum training with approved boxes
- Task ID: `REC-R09`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Train Vehicle at T1 for the current numeric maximum under the continuous policy, permitting boxes only for the explicit approved Vehicle continuation.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `TROOP-TRAINING-END-TO-END-CONSOLIDATION` (retain Vehicle T1/current-max queue and Home facts as historical evidence); `DQ-FLOW-TRAINING` (retain disabled four-way contract); `TOOLS-HOME-ATLAS-TROOP-TRAINING-ENTRY-MIGRATION` (reuse entry/navigation semantics, not its Train prohibition as current authorization).
- Evidence: `tasks/troop_training.py` validates current maximum and queue/resource facts; `tasks/troop_training_runtime.py` handles bounded start and unresolved reconciliation; `scripts/troop_training_bluestacks.py` owns retained route; product policy explicitly permits Vehicle T1 current maximum, continuous, boxes allowed, while keeping registration disabled.
- Scope: `tasks/troop_training.py`; `tasks/troop_training_runtime.py`; `scripts/troop_training_bluestacks.py`; `tests/test_troop_training.py`; `tests/test_training_disabled.py`
- Changes: Bind Vehicle facility/tab and T1; calculate the current numeric maximum; require exact observed cost/capacity and permit only the explicit Vehicle box continuation; persist continuous active queue and route through R01. Do not borrow Fighter values or silently use 250. Merge mutex: serial with REC-R06–REC-R08 over all shared training files and canonical registry hunks.
- Non-goals: No Fighter/Rider/Shooter, fixed-250 substitution, unknown box, Train Now, speedup, premium, or Daily inheritance.
- Acceptance: 1. Vehicle/T1/current numeric maximum/continuous facts all match one frame. 2. An absent/non-numeric maximum, unknown resource/cost, or unrecognized box blocks. 3. One start is reserved/dispatched/reconciled with queue/timer successor; no same-frame duplicate. 4. `UNKNOWN` is terminal pending observation and never auto-retries. 5. The separate Vehicle Main/Daily ownership boundary is preserved.
- Verification: Existing `python -m unittest tests.test_troop_training tests.test_training_disabled tests.test_flow_delivery_troop_training` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current Vehicle T1/current-max box-allowed scenario and verify exact queue/cost/box semantics and Home; retained consolidation evidence does not itself promote this route.
- Integration owner: Update shared troop registry/handler, Vehicle contract row in `tasks/gameplay_flow_contracts/TROOP-TRAINING-END-TO-END-CONSOLIDATION.json`, and `scripts/flow_delivery_troop_training_bluestacks.py:register` atomically under the disabled global gate.
- PR boundary: `recovery/rec-r09` — Vehicle training variant.
- Rollback: Disable Vehicle row and preserve R01; never restore an unbounded or wrong-box fallback.
- Runtime authorization: none.
- Completion: Vehicle variant code and focused proof do not imply native, scheduler, or production readiness.

### REC-R10 — Campaign destination navigation without AP execution
- Task ID: `REC-R10`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Provide one exclusive, zero-AP Home Atlas → Campaign → Story destination navigation route that recognizes exactly `1-20-9`, `1-15-9`, and `2-2-9`, then returns Home without Challenge, Auto Battle, AP, or any resource effect.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`
- Related work: `CAMPAIGN-ATLAS-NAVIGATION-INTEGRATION-AND-REPLAY` (reuse atlas/localization and zero-transport replay); `CAMPAIGN-AP-HOME-ATLAS-AND-DESTINATION-NAVIGATION` (retain all-three destination ground truth and navigation semantics, not old Completed status as canonical readiness); `AUTONOMY-SERVICE-CAMPAIGN-NAVIGATION-PROVING-SLICE` (historical navigation-only boundary); `CAMPAIGN-AND-ULTIMATE-CHALLENGE-FLOW-SCOPE-CORRECTION` (retain Campaign/Ultimate separation).
- Evidence: `tasks/campaign_auto_battle.py` allowlists the three tuples and rejects `1-2-9`/`ultimate-challenge`; `tasks/campaign_atlas_chapter_nav.py` and `tasks/campaign_atlas.py` provide atlas-directed navigation and current-frame binding; `scripts/flow_delivery_campaign_bluestacks.py` has separate navigation and Auto Battle runners; retained Stage-9 manifests provide historical native ground truth but do not authorize AP.
- Scope: `tasks/campaign_auto_battle.py`; `tasks/campaign_atlas_chapter_nav.py`; `tasks/campaign_atlas.py`; `scripts/flow_delivery_campaign_bluestacks.py`; `tests/test_campaign_atlas_chapter_nav.py`
- Changes: Keep Home Atlas Campaign entry as the only entry; bind difficulty/chapter/stage from current frames and atlas landmarks; expose destination verification as a navigation-only action plan; reject Ultimate Challenge and removed aliases; do not import AP ledger or Challenge actions. Merge mutex: serialize with REC-R11 because both use Campaign controller/script/registry files, while keeping the two policy outcomes separate.
- Non-goals: No AP consumption, Challenge, Hero Lineup, Auto Battle, refill, Buy Now, Ultimate Challenge, stage result, or resource/effect authority mutation.
- Acceptance: 1. Exactly the three supported tuples verify; `1-2-9` and `ultimate-challenge` fail closed. 2. Home Atlas entry, Story mode, difficulty, chapter, Stage 9, and destination identity are current-frame bound. 3. Navigation-only replay produces zero transport and never advances AP. 4. Ambiguous/stale/contradictory destination blocks without identical retry. 5. Canonical Home terminal is required after navigation or the route remains blocked.
- Verification: Existing `python -m unittest tests.test_campaign_atlas_chapter_nav tests.test_campaign_atlas_navigation tests.test_campaign_stage9_destination_replay tests.test_campaign_story_destinations` is prescribed after implementation; no command is run here.
- Native gate: `REC-O09` navigation pilot plus a route-specific `REC-O10` navigation scenario must re-prove current source/target/successor; retained BlueStacks destination_verified evidence is historical and is not AP or canonical production proof.
- Integration owner: Update `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, `automation_service/handlers.py`, `scripts/flow_delivery_campaign_bluestacks.py:run_campaign_navigation_only/register`, and Campaign navigation contract rows atomically with the global gate disabled; AP runner must remain a distinct inactive binding.
- PR boundary: `recovery/rec-r10` — Campaign navigation-only cutover.
- Rollback: Disable Campaign navigation row and preserve the atlas zero-transport path; do not restore open-loop Campaign entry.
- Runtime authorization: none.
- Completion: Navigation code/replay and exclusive wiring are separate from AP execution/native enablement.

### REC-R11 — Campaign AP Auto Battle at exact configured destinations
- Task ID: `REC-R11`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Execute only configured Story Auto Battle runs at `1-15-9` (14 AP), `1-20-9` (16 AP), or `2-2-9` (20 AP), capped at 120 AP with natural regeneration of exactly 1 AP per 360 seconds, and return Home without refill or mode substitution.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`, `REC-R10`
- Related work: `DQ-FLOW-CAMPAIGN-AP` (retain exact AP arithmetic and exclusions); `CAMPAIGN-AP-AUTO-BATTLE-LIVE-CANARY` (retain production-controller shape and historical AP/Home evidence, not old completion as canonical readiness); `DQ-FLOW-CAMPAIGN-AUTO-BATTLE` (reuse dormant controller/replay); `CAMPAIGN-AP-HOME-ATLAS-AND-DESTINATION-NAVIGATION` (destination dependency only, not execution authorization).
- Evidence: `tasks/flow_delivery_product_policy.json` fixes max AP 120, regeneration 360 seconds, and costs 14/16/20 with refills false; `tasks/campaign_auto_battle.py` defines the allowlist, exact parsing, bounded run arithmetic, explicit battle result and production replay; `tasks/campaign_auto_battle_runtime.py` provides the shared runtime controller; `tasks/campaign_auto_battle_vision.py` binds current stage/AP/result controls; retained canary records show historical native AP/Home behavior but remain non-canonical until current O10 acceptance.
- Scope: `tasks/campaign_auto_battle.py`; `tasks/campaign_auto_battle_runtime.py`; `tasks/campaign_auto_battle_vision.py`; `scripts/flow_delivery_campaign_bluestacks.py`; `tests/test_campaign_auto_battle_runtime.py`
- Changes: Reuse REC-R10's destination binding but keep AP execution as a separate runner; verify static cost on every stage dialog, displayed AP before each run, AP ≤120, natural regeneration independently, and exact Auto Battle/result/Home transitions. Forbid Sweep, Blitz, Auto Complete, refill, Buy Now, Ultimate Challenge, and identical Campaign r2 retries. Persist every effect through R01 and treat timeout/lost result as `UNKNOWN`, never as permission to replay. Merge mutex: serial with REC-R10 and any Campaign handler/registry edits.
- Non-goals: No navigation-only route ownership, AP refill, paid action, premium currency, unsupported destination, fixed-time success, or automatic repeat after ambiguous battle/result.
- Acceptance: 1. Only the three tuples and exact costs 14/16/20 are admitted. 2. AP ledger never exceeds 120 and every spend is reconciled to displayed before/after plus independent regeneration. 3. No run starts below its fresh cost; refills and substitutions are denied. 4. Explicit victory/defeat and final Home are required; timeout or unknown result blocks and never auto-retries. 5. R10 navigation remains separately testable and no old Campaign r2 binding can dispatch concurrently.
- Verification: Existing `python -m unittest tests.test_campaign_auto_battle tests.test_campaign_auto_battle_runtime tests.test_campaign_ap tests.test_campaign_stage9_destination_replay` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current route-specific AP scenario for each admitted destination or explicitly record the untested subset, with exact before/after AP, result, and Home. Historical Campaign canary evidence is reusable evidence, not current canonical acceptance.
- Integration owner: Update `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, `automation_service/handlers.py`, `scripts/flow_delivery_campaign_bluestacks.py:run_campaign_auto_battle_live/register`, and the AP gameplay contract/queue/registry hunks atomically only after R10's separate binding is retained; no navigation and AP runner may be live simultaneously for one occurrence.
- PR boundary: `recovery/rec-r11` — Campaign AP Auto Battle cutover.
- Rollback: Disable AP row and retain no refill/unsupported-mode policy; never restore Campaign r2 or an unbounded AP loop.
- Runtime authorization: none.
- Completion: Code and offline replay may merge separately from current native evidence and scheduler/production enablement.

### REC-R12 — Ruins zero-cost NPC challenge
- Task ID: `REC-R12`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Migrate the current-day Ruins NPC challenge route as a bounded zero-cost action with exact challenge identity, positive result, and canonical Home recovery, while keeping purchases, exchanges, AP, and premium actions out.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `DQ-FLOW-RUINS-CHALLENGE-BLUESTACKS` (retain the actual current-day zero-cost Gear Challenge result and Home recovery as historical evidence); `DQ-FLOW-CHALLENGES` (retain disabled challenge contract and Ultimate/Main separation). Home Atlas entry is reused from the retained implementation, not treated as a separate task dependency.
- Evidence: `tasks/ruins_challenge.py` defines known identities, current-day/zero-cost guards, attack/dispatch authorization, result verification, and chest separation; `tasks/ruins_challenge_runtime.py` provides bounded state; `scripts/ruins_challenge_bluestacks.py` owns retained route; `tests/test_ruins_challenge.py` and `tests/test_ruins_challenge_replay.py` cover identity/result boundaries. Historical BACKLOG evidence records Gear Floor 67→68 zero-cost challenge and Home, but no current canonical acceptance.
- Scope: `tasks/ruins_challenge.py`; `tasks/ruins_challenge_runtime.py`; `scripts/ruins_challenge_bluestacks.py`; `tests/test_ruins_challenge.py`; `tests/test_ruins_challenge_replay.py`
- Changes: Bind Ruins from Home Atlas, current-day enabled challenge row and exact detail/Attack/zero-cost NPC Dispatch semantics; require explicit result and return Home. Keep chest claims in R13 and shops in policy tickets. Route any consequential effect through R01 even though entry cost is zero. Unknown result/crash blocks with no repeated dispatch. Merge mutex: serialize with R13 over Ruins entry/recovery/registry paths.
- Non-goals: No level-60 or arbitrary combat, paid challenge, AP/stamina/currency use, purchases, exchange, chest Claim, or static Daily-row launch.
- Acceptance: 1. Identity, day, source, target, and zero cost are current-frame bound. 2. Only allowlisted NPC challenge/Dispatch controls are admitted. 3. Explicit result and positive progress are required; transport return alone is insufficient. 4. Ambiguous result is `UNKNOWN`/blocked and never automatically retried. 5. Canonical Home and ownership release follow success or safe block; R13 chest state remains independent.
- Verification: Existing `python -m unittest tests.test_ruins_challenge tests.test_ruins_challenge_replay tests.test_bluestacks_ultimate_challenge` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must later assign a current Ruins challenge scenario with exact identity, zero-cost proof, result, and Home; the retained 2026-08-11 native run is historical reusable evidence only.
- Integration owner: Update `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, `automation_service/handlers.py`, `scripts/ruins_challenge_bluestacks.py:register`, and the Ruins Challenge gameplay contract atomically; chest continuation remains a separately disabled binding.
- PR boundary: `recovery/rec-r12` — Ruins challenge-only cutover.
- Rollback: Disable challenge row and preserve no-cost/identity guards; do not restore a broad combat route.
- Runtime authorization: none.
- Completion: Offline route and atomic binding do not imply native acceptance or Daily Claim.

### REC-R13 — Ruins per-category chest maintenance and durable continuation
- Task ID: `REC-R13`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Maintain each independently ready Ruins chest category through BotStateManager-owned SQLite state, separate from challenge, ordinary Claim, milestone Claim, and Shop ownership. The existing JSON continuation is retained only as non-authorizing history/export; it must not remain runtime authority.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `DQ-FLOW-RUINS-CHALLENGE-BLUESTACKS` (retain observed challenge/chest separation and any historical claims as evidence only); `DQ-FLOW-CHALLENGES` (retain disabled chest/result boundaries). The retained chest-continuation contract is in the named implementation file, not a separate legacy task ID.
- Evidence: `tasks/ruins_challenge_continuation.py` fixes the legacy JSON schema/version, canonical identities, evidence-root containment, reset binding, and confirmed claims; `tasks/ruins_challenge.py` separates `RuinsChestClaim` and postcondition from challenge; `automation_service/state.py` is the canonical SQLite `BotStateManager` authority; `scripts/ruins_challenge_bluestacks.py` has bounded chest scroll/claim route; existing continuation/state tests cover integrity and durable-state seams. Historical JSON is reusable for semantics/exports only, never as current eligibility or retry authority.
- Scope: `automation_service/state.py`; `tasks/ruins_challenge_continuation.py`; `tasks/ruins_challenge.py`; `scripts/ruins_challenge_bluestacks.py`; `tests/test_ruins_challenge_continuation.py`
- Changes: Extend `automation_service/state.py:BotStateManager` with SQLite-owned per-reset, per-category progress/claimed/`UNKNOWN` state and atomic read/transition APIs; wire the chest route to read eligibility and retry state only from SQLite, route confirmed effects through R01, and retain unresolved effects without erasure. Keep the legacy JSON validator/serializer for non-authorizing history/export only: never import conflicting historical JSON into current state, and never use edited, deleted, malformed, or stale JSON to grant eligibility or clear `UNKNOWN`. Restart must reopen the same SQLite facts without duplicating claims. Do not make challenge success a prerequisite for an already-ready chest, and do not share purchase or Daily Claim handlers. Merge mutex: serial with R12 over Ruins script/registry/entry hunks and with state-owner changes.
- Non-goals: No JSON runtime authority or JSON-to-SQLite import, NPC Challenge dispatch, ordinary/milestone Daily Claim, exchange, Ruins Shop Buy, unknown category, cross-reset state reuse, or protected evidence rewrite.
- Acceptance: 1. Each category has independent identity, readiness, target, reset, progress, claim, and effect-outcome state in BotStateManager SQLite. 2. Restart preserves confirmed claims and unresolved `UNKNOWN`/reconciliation state; a duplicate claim cannot be created. 3. Edited, deleted, malformed, digest-invalid, or conflicting JSON cannot alter current eligibility, retry, or unresolved-effect state; no historical JSON is imported. 4. Unknown post-claim effect is retained for reconciliation, first semantically resolved to `NO_EFFECT` or another explicit terminal outcome, and never automatically retried; a changed hypothesis is valid only after that resolution. 5. Challenge, chest, Daily Claim, and Shop owners remain distinct and final Home is required.
- Verification: Existing `python -m unittest tests.test_automation_service_state tests.test_ruins_challenge_continuation tests.test_ruins_challenge tests.test_ruins_challenge_replay` is prescribed after implementation; no command is run here.
- Native gate: `REC-O10` must assign a current chest-maintenance scenario per category that is actually admitted, including fresh readiness, one claim, effect/result, SQLite per-reset/category durability across restart, and Home; historical claims and JSON checkpoints are not current acceptance.
- Integration owner: State owner owns the SQLite schema/API in `automation_service/state.py:BotStateManager._create_schema` and corresponding state-manager methods; route owner wires `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, `automation_service/handlers.py`, and `scripts/ruins_challenge_bluestacks.py` to that authority, while retaining `tasks/ruins_challenge_continuation.py` only for validated history/export. Chest continuation fields in `tasks/gameplay_flow_contracts/RUINS-CHALLENGE-HOME-ATLAS-MIGRATION.json` remain contract metadata, not a competing runtime store; this is a separate disabled cutover from R12.
- PR boundary: `recovery/rec-r13` — Ruins chest maintenance only.
- Rollback: Disable chest maintenance and preserve BotStateManager SQLite rows, including unresolved `UNKNOWN`/reconciliation state; do not fall back to JSON, challenge, or claim routes.
- Runtime authorization: none.
- Completion: SQLite-owned per-reset/category state and chest ownership can merge only as a distinct PR; JSON remains non-authorizing history/export, and native evidence/enablement remain separate.

### REC-R14 — Nano Material Production six-hour maintenance
- Task ID: `REC-R14`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Add a separate Nano Material Production maintenance route that claims completed production and starts the next when idle, refreshes active due time, uses exactly one 21600-second production, and consumes no base resource, box, currency, or item.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `NANO-MATERIAL-PRODUCTION-MAINTENANCE` (retain the explicit maintenance contract; historical Blocked status means implementation/evidence is missing); `DQ-FLOW-NANOWEAPON` (retain Nano Material → Nanoweapon ordering and do not claim Craft here).
- Evidence: `tasks/nanoweapon.py` retains the exact Nanoweapon screen/tab vocabulary and native profile guard; `tasks/daily_nanoweapon.py` demonstrates the separate Daily Craft adapter; product policy fixes Material Production at one active production, 21600 seconds, zero resource cost, and Home terminal. No dedicated Material Production runtime module or script currently exists; those are explicit NEW paths below.
- Scope: `tasks/nanoweapon.py`; `tasks/nano_material_production.py (NEW)`; `scripts/nano_material_production_bluestacks.py (NEW)`; `tests/test_nano_material_production.py (NEW)`; `automation_service/actions.py`
- Changes: Create only the NEW maintenance controller/adapter and reuse Nanoweapon screen identity; model `completed → claim → next start`, `active → due refresh`, and `idle → start` with one active slot and exact 21600 seconds; route any completion/resource effect through R01 and return Home. Unknown production state, timer, result, or effect blocks and never retries. Merge mutex: serialize with R15 over Nanoweapon screen/registry/handler hunks; do not make R15's Craft semantics a prerequisite for maintenance.
- Non-goals: No Nanoweapon Craft, Exclusive Craft, parts consumption, paid action, box, arbitrary duration, second active production, Daily Claim, or policy promotion.
- Acceptance: 1. Exactly one active Material Production and 21600-second duration are enforced. 2. Claim/start occurs only with current-frame identity and known zero cost; active state only refreshes due metadata. 3. Completion/effect is reconciled through R01; `UNKNOWN` never auto-retries or starts another production. 4. Restart preserves one active slot and no duplicate start. 5. Canonical Home is required after each bounded pass or the route blocks.
- Verification: Existing `python -m unittest tests.test_nanoweapon tests.test_daily_nanoweapon` plus NEW `python -m unittest tests.test_nano_material_production` is prescribed after implementation; the NEW test module is not present today and no command is run here.
- Native gate: `REC-O10` must later assign a current Material Production maintenance scenario with exact timer, zero-cost proof, claim/start or due-refresh outcome, and Home; current evidence is absent and no native success is claimed.
- Integration owner: Canonical service owner must add the NEW flow row in `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, the disabled handler in `automation_service/handlers.py`, exact existing gameplay contract `tasks/gameplay_flow_contracts/NANO-MATERIAL-PRODUCTION-MAINTENANCE.json`, and remove/hard-disable any legacy Nanoweapon maintenance binding atomically.
- PR boundary: `recovery/rec-r14` — Nano Material maintenance route.
- Rollback: Disable the new maintenance row and retain no competing Nanoweapon maintenance dispatcher; preserve R01 state.
- Runtime authorization: none.
- Completion: New offline controller, focused tests, and exclusive wiring are required; no Craft or native enablement is implied.

### REC-R15 — Nanoweapon Normal Craft one start per reset
- Task ID: `REC-R15`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Start at most one Nanoweapon Normal Craft per reset only when exactly 100 nano parts and an enabled Craft control are positively observed, with a single active 43200-second craft and no Exclusive Craft.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`, `REC-R14`
- Related work: `DQ-FLOW-NANOWEAPON` (retain exact Normal Craft/100 parts/43200 seconds/one-reset semantics and offline tests; Passed is not native readiness); `NANO-MATERIAL-PRODUCTION-MAINTENANCE` (R14 maintenance precedes this route; do not fold the two operations together).
- Evidence: `tasks/nanoweapon.py` defines `NANO_PARTS_REQUIRED=100`, `NORMAL_CRAFT_DURATION_SECONDS=43200`, Normal Craft authorization/transaction/postcondition; `tasks/daily_nanoweapon.py` separates selected-Daily row and Craft progress; `tests/test_nanoweapon.py` and `tests/test_daily_nanoweapon.py` cover exact recipe/cardinality; product policy forbids Exclusive Craft and permits one start/reset. No native Nanoweapon route script currently exists.
- Scope: `tasks/nanoweapon.py`; `tasks/daily_nanoweapon.py`; `scripts/nanoweapon_bluestacks.py (NEW)`; `tests/test_nanoweapon.py`; `tests/test_daily_nanoweapon.py`
- Changes: Reuse exact Normal Craft contract after R14 maintenance; bind selected Nanoweapon/Normal Craft and parts balance; require ≥100 observed parts but consume exactly 100, one active craft, one start per reset, and exact 43200-second timer; claim completed weapon on entry only when positively ready. Route effect through R01, preserve Daily Claim separation, and block on unknown material/timer/result. Merge mutex: serialize with R14 over Nanoweapon screen/handler/registry; do not share a Craft and Material Production dispatch loop.
- Non-goals: No Exclusive Craft, rotating display, paid/10x craft, parts substitution, second active craft, second same-reset start, resource box, or Claim ownership.
- Acceptance: 1. Only Normal Craft, exact 100 parts, and 43200 seconds authorize. 2. A completed craft is claimed once on entry; an active craft is not restarted. 3. Reset state prevents a second start after restart. 4. Unknown Craft result/effect is blocked and never auto-retried; it must be semantically reconciled to `NO_EFFECT` before any new action, while any other terminal outcome closes the occurrence. Only a `NO_EFFECT` retry with an explicit changed hypothesis and authorization may proceed. 5. Daily progress/Claim remains separately owned and Home is required after bounded work.
- Verification: Existing `python -m unittest tests.test_nanoweapon tests.test_daily_nanoweapon` plus NEW `python -m unittest tests.test_nanoweapon_bluestacks` is prescribed after implementation; `tests/test_nanoweapon_bluestacks.py` is NEW and no command is run here.
- Native gate: `REC-O10` must later assign current Native Normal Craft evidence with exact parts/timer/one start, effect, and Home; retained offline contracts and any old labels do not satisfy it.
- Integration owner: Add the NEW script runner and exact contract wiring through `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, `automation_service/handlers.py`, and `tasks/gameplay_flow_contracts/NANOWEAPON-BLUESTACKS-INTEGRATION.json`; keep R14 maintenance binding separate and atomically hard-disable legacy Craft dispatch.
- PR boundary: `recovery/rec-r15` — Nanoweapon Normal Craft route.
- Rollback: Disable Craft row, preserve parts/effect authority, and never restore Exclusive/legacy Craft dispatch.
- Runtime authorization: none.
- Completion: Craft code and offline proof do not imply current native acceptance, Daily completion, or scheduler enablement.

### REC-R16 — Shared direct Home-to-World gathering/march controller
- Task ID: `REC-R16`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Build one shared, bounded direct Home → World → Search → exact level-5 node → free-slot/default-formation march → return controller for resource variants, with Food limited to a proving slice and never admitted as selected-Daily ownership.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `DQ-FLOW-WORLD-STAMINA-ENGINE` (retain World/resource accounting and no-dispatch boundary); `DQ-FLOW-GATHERING` (retain parameterized Wood/Steel/Gas contract and exact variants, not its Passed label as native readiness). The retained navigation implementation remains a shared code seam, not a separate legacy task ID.
- Evidence: `tasks/world_stamina.py` provides navigation observations, current-frame node binding, bounded pan/search plans, and explicit no-dispatch separation; `tasks/gathering.py` defines exact variant identity, free node/march guards, transaction/postcondition, and quantity map; `scripts/world_map_navigation_bluestacks.py` is the retained navigation-only owner. Product policy requires direct Home-to-World flow, free node/slot/default formation, exact level 5, no existing march override, and separate Daily progress.
- Scope: `tasks/world_stamina.py`; `tasks/gathering.py`; `tasks/gathering_runtime.py (NEW)`; `scripts/gathering_bluestacks.py (NEW)`; `tests/test_gathering_runtime.py (NEW)`
- Changes: Reuse World navigation facts and gathering semantic contracts; create one NEW shared controller that binds Home/World/Search/category/level5/node/slot/formation from current frames, dispatches one bounded march, records outbound/return timing, and reconciles resource effect via R01. Keep Food as a bounded proving march only; it must not create a selected-Daily prerequisite or Claim owner. Reject occupied/already-targeted nodes and existing march overrides. Merge mutex: R16 owns the shared controller; R17/R18 are serial variants and must not force unrelated policy semantics into this engine.
- Non-goals: No variant-specific quantities beyond shared binding, attack, zombie lair, Campaign, AP/stamina substitution, arbitrary node level, occupied node, existing march override, premium, or selected-Daily Food admission.
- Acceptance: 1. Direct route order and exact level-5/free-node/free-slot/default-formation facts are enforced. 2. One march maximum per occurrence with positive outbound and return/result evidence. 3. Occupied, targeted, stale, unknown, or contradictory node/formation/slot blocks. 4. Crash/unknown march effect is reconciled or blocked and never automatically repeated. 5. Food proving evidence is explicitly non-Daily; no resource variant silently gains Daily route authority.
- Verification: Existing `python -m unittest tests.test_world_stamina tests.test_gathering tests.test_list_search tests.test_world_map_navigation_bluestacks` plus NEW `python -m unittest tests.test_gathering_runtime` is prescribed after implementation; the NEW test module is not present and no command is run here.
- Native gate: `REC-O08` hosted-native World foundation evidence is required only before any later native dispatch, route acceptance, or scheduler enablement; it does not block this offline-safe controller code/replay acceptance under the existing `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, and `REC-R01` dependencies. Missing `REC-O08` or per-route `REC-O10` evidence cannot authorize inputs, but must not block safe offline implementation. Native enablement later requires `REC-O08` plus a separate current `REC-O10` scenario for each route. Existing HUD-only/navigation and synthetic gathering evidence is not native march/effect acceptance.
- Integration owner: Add the NEW shared gathering runner and NEW contract `tasks/gameplay_flow_contracts/GATHERING-SHARED-MARCH.json` through `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, `automation_service/handlers.py`, and the exact `scripts/world_map_navigation_bluestacks.py` Home/World owner hunk; old navigation remains zero-input and cannot dispatch a march.
- PR boundary: `recovery/rec-r16` — shared gathering/march controller only.
- Rollback: Disable all gathering rows and preserve World navigation-only behavior plus R01; never restore an open-loop march path.
- Runtime authorization: none.
- Completion: Shared controller/replay and exclusive wiring are complete before R17/R18 variant PRs, but native/effect enablement remains separate.

### REC-R17 — Wood 30000 and Steel 6000 gathering variants
- Task ID: `REC-R17`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Add independent Wood quantity 30000 and Steel quantity 6000 route variants on REC-R16's shared march controller, each with its own observable target/effect acceptance and no selected-Daily prerequisite.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`, `REC-R16`
- Related work: `DQ-FLOW-GATHERING` (retain exact Wood/Steel quantities and parameterized guards; historical Passed is not current native readiness); `DQ-FLOW-WORLD-STAMINA-ENGINE` (retain World navigation/resource boundary).
- Evidence: `tasks/gathering.py` defines `SUPPORTED_VARIANTS` and `_variant_identity` quantity mapping, exact node/slot/occupancy/formation authorization, transaction specs, and postconditions; `tasks/world_stamina.py` supplies current-frame World/node binding; `tests/test_gathering.py` covers Wood/Steel identity and quantity arithmetic; product policy lists Wood 30000 and Steel 6000 with no selected-Daily prerequisite.
- Scope: `tasks/gathering.py`; `tasks/gathering_runtime.py (NEW)`; `scripts/gathering_bluestacks.py (NEW)`; `tests/test_gathering.py`; `tests/test_gathering_runtime.py (NEW)`
- Changes: After R16 is merged, add two independently selectable variant records to the shared engine: Wood exact 30000 and Steel exact 6000. Bind resource category, level 5 node, free slot, default formation, outbound/return timing, and effect delta; persist variant-specific occurrence/effect through R01. Merge mutex: serial with R16 and R18 over `tasks/gathering.py`, NEW runtime/script/tests, registry, and contract; no variant may dispatch while a stale shared file remains.
- Non-goals: No Gas reveal/rebind, Food Daily admission, arbitrary quantity/node level, occupied node, march override, premium, attack, stamina/AP spend, or variant aliasing.
- Acceptance: 1. Wood and Steel are separate observable variants with exact quantities 30000 and 6000. 2. Current-frame identity, level 5/free node/free slot/default formation, and positive return/effect are required. 3. A variant cannot satisfy another variant's occurrence or quantity. 4. Unknown march/effect never auto-retries; unresolved state blocks. 5. Progress is observed separately and never creates selected-Daily ownership.
- Verification: Existing `python -m unittest tests.test_gathering tests.test_world_stamina` plus NEW `python -m unittest tests.test_gathering_runtime` is prescribed after implementation; the NEW module is not present and no command is run here.
- Native gate: `REC-O10` must assign one current Wood scenario and one current Steel scenario independently, each with exact quantity/effect and Home; prior parameterized/synthetic results remain historical context.
- Integration owner: Update only the Wood/Steel rows in NEW contract `tasks/gameplay_flow_contracts/GATHERING-SHARED-MARCH.json`, `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, and `automation_service/handlers.py`; preserve R16 shared owner and keep R18 Gas row inactive until its own merge.
- PR boundary: `recovery/rec-r17` — Wood/Steel gathering variants.
- Rollback: Disable Wood/Steel rows while retaining R16 zero-dispatch controller and R01; do not revert to legacy march code.
- Runtime authorization: none.
- Completion: Both variants have independent offline acceptance; native scenarios and scheduler enablement remain separate.

### REC-R18 — Gas 1500 gathering with bounded reveal/rebind
- Task ID: `REC-R18`
- Type: Story
- Priority: P2
- Status: WAITING_DEPENDENCIES
- Objective: Add the Gas quantity 1500 variant to REC-R16's shared march controller, revealing Gas only through one bounded left swipe and rebinding the target from the immediate current frame before dispatch.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`, `REC-R16`
- Related work: `DQ-FLOW-GATHERING` (retain Gas 1500 variant and bounded-reveal policy; historical Passed is not current native readiness); `DQ-FLOW-WORLD-STAMINA-ENGINE` (retain World/search/node safety).
- Evidence: `tasks/gathering.py` defines Gas variant identity, exact quantity and node/slot/occupancy guards; `tasks/world_stamina.py` provides bounded pan/search and current-frame node binding; `tests/test_gathering.py` covers Gas and stale/occupied negatives; product policy requires one bounded left swipe and immediate rebind, with no selected-Daily prerequisite. No native current Gas march proof is claimed.
- Scope: `tasks/gathering.py`; `tasks/gathering_runtime.py (NEW)`; `scripts/gathering_bluestacks.py (NEW)`; `tests/test_gathering.py`; `tests/test_gathering_runtime.py (NEW)`
- Changes: Add only Gas 1500 variant after R16; if Gas is not visible, allow one bounded left swipe, invalidate perception, and require a fresh current-frame Gas level-5/free-node binding. Dispatch one default-formation march and reconcile outbound/return/effect via R01. Any second reveal, stale rebind, occupied node, unknown duration/effect, or crash blocks. Merge mutex: serial with R16/R17 over shared gathering engine and integration hunks.
- Non-goals: No repeated reveal/pan loop, Wood/Steel aliasing, Food Daily admission, arbitrary node/quantity, march override, attack, premium, AP/stamina spend, or automatic unknown retry.
- Acceptance: 1. Gas identity and exact 1500 quantity are required. 2. At most one left-swipe reveal is permitted and it never authorizes a target without fresh rebind. 3. Node/slot/formation and outbound/return/effect proof are current-frame bound. 4. Unknown reveal, march, or effect blocks and never repeats automatically. 5. Gas progress is observed separately from selected-Daily ownership.
- Verification: Existing `python -m unittest tests.test_gathering tests.test_world_stamina` plus NEW `python -m unittest tests.test_gathering_runtime` is prescribed after implementation; the NEW test module is not present and no command is run here.
- Native gate: `REC-O10` must assign a current Gas scenario covering one reveal, fresh rebind, exact 1500 result, and Home; no existing synthetic or generic World evidence substitutes.
- Integration owner: Update only the Gas row in NEW contract `tasks/gameplay_flow_contracts/GATHERING-SHARED-MARCH.json`, `automation_service/registry.py:CANONICAL_FLOW_REGISTRY`, and `automation_service/handlers.py`; retain R16 owner and R17 independent rows, with disabled-gate serial merge.
- PR boundary: `recovery/rec-r18` — Gas gathering variant.
- Rollback: Disable Gas row, preserve shared controller and R01, and do not restore an open-loop reveal/rebind path.
- Runtime authorization: none.
- Completion: Gas variant code/fixtures are separate from shared controller and native acceptance/enablement.

### REC-R19 — Defer Zombie Lair maintenance and Daily attribution pending product decision
- Task ID: `REC-R19`
- Type: Decision
- Priority: P3
- Status: BLOCKED_POLICY
- Objective: Resolve whether the deferred notification-driven Zombie Lair maintenance capability may be authorized at all; until an explicit product decision is recorded, permit observation only and deny every join, stamina spend, refill, and Daily attribution.
- Dependencies: none
- Related work: `ZOMBIE-LAIR-HOME-MAINTENANCE` (retain the notification-driven contract but its Blocked status and no-registration state); `DQ-FLOW-ZOMBIE-LAIR` (retain first-successful-eligible-join Daily observation contract, not dispatch readiness); `DQ-FLOW-STAMINA` (retain disabled stamina arithmetic); `DQ-FLOW-WORLD-STAMINA-ENGINE` (retain World/stamina safety).
- Evidence: `tasks/zombie_lair.py` and `tasks/daily_zombie_lair.py` model exact level/stamina/result facts; `tasks/world_stamina.py` provides World guard; `tasks/flow_delivery_product_policy.json` records proposed levels 30–55, exactly 28 stamina, Quick Join, no refill, and first-success-per-reset attribution, while historical BACKLOG explicitly defers this capability. The conflict is policy state, not evidence of execution; no route implementation is currently authorized.
- Scope: `tasks/zombie_lair.py`; `tasks/daily_zombie_lair.py`; `tasks/world_stamina.py`; `tests/test_zombie_lair.py`; `tests/test_daily_zombie_lair.py`
- Changes: Record an explicit product decision with one concrete result: **allowed** means Home notification pulse only, levels 30–55, Quick Join, exactly 28 stamina, at most `min(eligible_lair_count, floor(current_stamina/28))`, no refill, maintenance continues after the first success, and first successful eligible join owns Daily completion; **denied** means zero join/consume/refill input and observation-only. If allowed, open a separate fully specified implementation ticket; this ticket does not implement or authorize it. Merge mutex: none for policy record, but any later route must serialize with R16 and R01.
- Non-goals: No implementation, native observation, stamina consumption, join, refill, combat/formation choice, Daily Claim, or inference that current policy JSON approval makes this route execution-ready.
- Acceptance: 1. The product owner records allowed or denied, exact level range, cost, count formula, Quick Join/formation rule, notification trigger, terminal Home state, and Daily attribution. 2. Missing or contradictory decision leaves all dispatch denied. 3. No existing historical native/synthetic result is promoted to current acceptance. 4. If allowed, a new implementation ticket names route/test/integration paths before work begins. 5. Any unknown join/result/stamina effect is blocked and never automatically retried.
- Verification: Existing `python -m unittest tests.test_zombie_lair tests.test_daily_zombie_lair tests.test_world_stamina` is prescribed to check the retained disabled contract after a decision; no command is run here.
- Native gate: None while BLOCKED_POLICY; after a separate implementation ticket, `REC-O10` must assign current supervised scenarios and no old generic World join evidence may satisfy them.
- Integration owner: Product authority owner updates only the exact Zombie policy record and disposition; no `automation_service/registry.py` or scheduler row may be enabled by this decision ticket. A later implementation owner must provide exact registry/handler/contract hunks.
- PR boundary: `recovery/rec-r19` — policy decision record only.
- Rollback: Leave dispatch disabled and preserve the deferred blocker; never re-enable a legacy Zombie Lair route.
- Runtime authorization: none.
- Completion: Product decision recorded or blocker preserved; neither outcome claims implementation, native readiness, or Daily completion.

### REC-P01 — Decide Ruins Shop purchase policy
- Task ID: `REC-P01`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Resolve the Ruins Shop candidate of one three-star Chip material for 15 `RUINS_COINS` without authorizing a Buy action from incomplete offer or balance evidence.
- Dependencies: none
- Related work: `DQ-FLOW-PURCHASES` (retain disabled four-shop arithmetic and no-dispatch contract); `DQ-FLOW-CHALLENGES` (keep challenge and Shop ownership separate). The historical product-policy/evidence gap is retained in the source policy record; no separate Ruins Shop gate task exists.
- Evidence: `tasks/purchases_disabled.py` exposes exact shop/offer/currency/quantity recognition and always-block dispatch; `tasks/flow_delivery_product_policy.json` records `three_star_chip_material`, `RUINS_COINS`, cost 15, quantity one, but status `unresolved_user_decision` and missing current identity/balance/successor; `tests/test_purchases_disabled.py` covers arithmetic and disabled behavior.
- Scope: `tasks/purchases_disabled.py`; `tests/test_purchases_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Product owner must choose allowed or denied. Allowed must name current item identity, `RUINS_COINS`, exact cost 15, quantity one, sufficient balance, no premium/refill, successor, and Home; denied must explicitly prohibit Buy. Until recorded, preserve block. A later implementation ticket must specify route and integration paths; this ticket is not an implementation.
- Non-goals: No offer inspection input, Buy, exchange, currency spend, resource mutation, or native evidence collection.
- Acceptance: 1. Decision names allow/deny and every required identity/cost/balance/successor field. 2. Unknown or changed offer/cost/currency remains denied. 3. Quantity cannot exceed one and premium currency is always denied. 4. Decision does not alter registration/scheduler or claim policy. 5. Any future allowed route receives a separate implementation ticket and unknown effects never auto-retry.
- Verification: Existing `python -m unittest tests.test_purchases_disabled` is prescribed after policy-schema update; no command is run here.
- Native gate: none; policy decision cannot satisfy the later `REC-O10` current native Shop evidence gate.
- Integration owner: Product-policy owner updates the exact `rare/ruins shop` policy record only; no route registry/handler/queue mutation is permitted. Future implementation must own exact registry/contract hunks.
- PR boundary: `recovery/rec-p01` — Ruins Shop policy decision.
- Rollback: Restore explicit denied/unknown state and keep Buy unavailable; never reactivate a legacy purchase adapter.
- Runtime authorization: none.
- Completion: A recorded product decision is complete as policy only; no implementation or purchase is considered done.

### REC-P02 — Decide Rare Earth Shop purchase policy
- Task ID: `REC-P02`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Resolve whether one current three-star Rare Earth Shop item may be purchased, including exact item, currency, cost, balance, and successor, while denying Buy until that contract exists.
- Dependencies: none
- Related work: `DQ-FLOW-PURCHASES` (retain disabled four-shop model). The historical product-policy/evidence gap is retained in the source policy record; no separate Rare Earth Shop gate task exists.
- Evidence: `tasks/purchases_disabled.py` has `OBJECTIVE_TO_SHOP` and strict offer/cost guards; product policy status is `unresolved_user_decision` with unknown current item/currency/cost and quantity one; `tests/test_purchases_disabled.py` asserts disabled dispatch and changed-offer blocking.
- Scope: `tasks/purchases_disabled.py`; `tests/test_purchases_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Record allowed or denied. Allowed must specify the current three-star item, non-premium currency, exact cost/balance, quantity one, source/target/successor/Home evidence contract, and no refill; denied explicitly blocks Buy. Do not fill unknowns from a stale label. A future implementation ticket is required after approval.
- Non-goals: No Shop navigation input, Buy, currency spend, offer selection, Pit income, or scheduler/registry change.
- Acceptance: 1. Decision contains exact item/currency/cost/quantity and allowed or denied outcome. 2. Any unknown/changed offer or insufficient balance denies. 3. Premium/paid substitutions and quantity >1 remain denied. 4. Policy resolution does not claim native proof or implementation. 5. Future route work is a separately detailed ticket; unknown effect never auto-retries.
- Verification: Existing `python -m unittest tests.test_purchases_disabled` is prescribed after the policy record changes; no command is run here.
- Native gate: none; later allowed implementation still needs current `REC-O10` Shop evidence.
- Integration owner: Product-policy owner edits the exact Rare Earth Shop record only; no `automation_service` registration, queue, or handler hunk is in scope.
- PR boundary: `recovery/rec-p02` — Rare Earth Shop policy decision.
- Rollback: Keep Buy denied and retain unknown offer/cost blocker; do not restore a legacy shop dispatcher.
- Runtime authorization: none.
- Completion: Policy disposition only; no Shop action is complete.

### REC-P03 — Decide Alliance Shop purchase policy
- Task ID: `REC-P03`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Resolve whether an Alliance Shop purchase may occur by naming the current Joy Coin or Alliance Coin offer, exact cost/balance, quantity, and positive successor; deny Buy while unresolved.
- Dependencies: none
- Related work: `DQ-FLOW-PURCHASES` (retain disabled shop model); `DQ-FLOW-DONATION` (keep Shop purchase separate from Alliance Tech donation). The historical product-policy/evidence gap is retained in the source policy record; no separate Alliance Shop gate task exists.
- Evidence: Product policy `alliance-shop-purchase-policy` is `unresolved_user_decision` with unknown item/currency/cost and quantity one; `tasks/purchases_disabled.py` strictly recognizes shop/offer/currency and always blocks dispatch; `tests/test_purchases_disabled.py` covers unknown/ambiguous negatives.
- Scope: `tasks/purchases_disabled.py`; `tests/test_purchases_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Record allow/deny and exact current item, currency (`Joy Coin` or `Alliance Coin`), cost, sufficient balance, quantity one, successor/Home, and no premium/refill. Until then no Buy. If allowed, open a separate implementation ticket with route, test, and atomic registry/handler hunks.
- Non-goals: No purchase, donation, Alliance Tech resource use, offer probing, balance mutation, or scheduler/registration change.
- Acceptance: 1. Exact offer/currency/cost/balance/quantity and allow/deny are recorded. 2. Unknown or changed offer/currency denies. 3. Quantity one and non-premium only; no fallback currency. 4. Decision does not claim route implementation/native acceptance. 5. Future code ticket is mandatory and unknown effect never auto-retries.
- Verification: Existing `python -m unittest tests.test_purchases_disabled tests.test_donation_disabled` is prescribed after policy update; no command is run here.
- Native gate: none; a later code route needs current `REC-O10` Shop evidence.
- Integration owner: Product-policy owner updates only the exact Alliance Shop policy record; implementation owner later owns all registry/handler/contract integration hunks.
- PR boundary: `recovery/rec-p03` — Alliance Shop policy decision.
- Rollback: Preserve explicit deny/unknown state; never restore a purchase or donation bypass.
- Runtime authorization: none.
- Completion: Policy outcome only; no Alliance Shop action is implemented or done.

### REC-P04 — Resolve Hero Upgrade identity and no-diamond policy
- Task ID: `REC-P04`
- Type: Decision
- Priority: P1
- Status: BLOCKED_POLICY
- Objective: Resolve the `Wali` versus `Wally` identity and exact standard-material level/cost contract, with no silent 150-diamond exception, before any Hero Upgrade route can be authorized.
- Dependencies: none
- Related work: `DQ-FLOW-HERO-UPGRADE` (retain disabled selected-hero/material arithmetic and the Wali/Wally/product-evidence ambiguity).
- Evidence: `tasks/hero_upgrade_disabled.py` strictly separates selected hero, material, level, cost, successor, and disabled dispatch; product policy currently records candidate `Wali`, standard material, three per reset, and a historical 150-diamond reset exception while dispatch remains false; BACKLOG/product records also contain Wally ambiguity. This ticket must not silently adopt the conflicting record.
- Scope: `tasks/hero_upgrade_disabled.py`; `tests/test_hero_upgrade_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Product owner must select canonical identity (`Wali` or `Wally`), standard material identity and exact amount/cost, per-reset quantity (candidate three), and explicitly decide the exception. This recovery contract defaults to **no 150-diamond exception**: an allowed result must deny diamond spend and require standard material only; a denied result blocks all upgrades. After decision, open a separate implementation ticket with native target/effect and atomic integration paths.
- Non-goals: No hero selection, upgrade, diamond spend, material probing, identity alias, native evidence collection, or registry/scheduler mutation.
- Acceptance: 1. Exactly one hero spelling and standard-material amount/cost are recorded. 2. Three-per-reset (or an explicitly changed quantity) is concrete. 3. 150-diamond exception is explicitly denied or separately approved; absent explicit approval it remains denied. 4. Unknown hero/material/cost/level/successor blocks. 5. Decision does not claim implementation or native readiness; future code is a separate ticket and UNKNOWN never auto-retries.
- Verification: Existing `python -m unittest tests.test_hero_upgrade_disabled` is prescribed after the policy record is resolved; no command is run here.
- Native gate: none; a later implementation still requires current `REC-O10` Hero Upgrade scenario and no old policy record is native proof.
- Integration owner: Product-policy owner updates only the exact Hero Upgrade policy record; no handler, registry, queue, or scheduler changes. A later implementation ticket must own those exact hunks.
- PR boundary: `recovery/rec-p04` — Hero Upgrade policy and identity decision.
- Rollback: Keep Hero Upgrade disabled and deny diamond/material fallbacks; never restore ambiguous Wali/Wally dispatch.
- Runtime authorization: none.
- Completion: Product decision only; route remains disabled until separately specified and accepted.

### REC-P05 — Decide Hero Duel participation policy
- Task ID: `REC-P05`
- Type: Decision
- Priority: P1
- Status: BLOCKED_POLICY
- Objective: Resolve whether Hero Duel PvP participation is allowed; until an explicit event, opponent, attempt, cost, loss, and completion contract exists, deny entry, lineup changes, combat, and all consequences.
- Dependencies: none
- Related work: `DQ-FLOW-HERO-DUEL` (retain disabled event/identity contract and unresolved PvP decision).
- Evidence: `tasks/hero_duel_disabled.py` recognizes event/participation facts but `hero_duel_disabled_dispatch` always blocks; product policy explicitly marks PvP prohibited with unknown event/attempt/successor and daily candidate three. Existing evidence does not establish current free-opponent or cost semantics.
- Scope: `tasks/hero_duel_disabled.py`; `tests/test_hero_duel_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Record allowed or denied. Allowed must name current event, free-opponent/lineup rule, attempt count, cost (including zero/premium status), result/loss handling, per-reset quantity, successor/Home, and explicit PvP authorization. Denied keeps all entry/combat/lineup inputs blocked. Any allowed result needs a separate implementation ticket; this decision never authorizes dispatch.
- Non-goals: No event inspection input, PvP entry, lineup selection, combat, loss, reward, premium, or registration/scheduler change.
- Acceptance: 1. Product owner records allow/deny and exact event/opponent/cost/attempt/result contract. 2. Unknown event, cost, opponent, or consequence remains prohibited. 3. Denial blocks all PvP surfaces, not only final attack. 4. Policy decision does not claim native proof or implementation. 5. Future implementation is a new ticket and unknown outcome never retries automatically.
- Verification: Existing `python -m unittest tests.test_hero_duel_disabled` is prescribed after policy update; no command is run here.
- Native gate: none; any future allowed route requires current `REC-O10` PvP scenario after explicit authorization.
- Integration owner: Product-policy owner changes only the Hero Duel policy record; no service registry/handler/scheduler mutation is allowed.
- PR boundary: `recovery/rec-p05` — Hero Duel policy decision.
- Rollback: Preserve PvP prohibition and deny the entire route; do not restore a legacy event adapter.
- Runtime authorization: none.
- Completion: Policy disposition only; no Hero Duel participation is implemented or done.

### REC-P06 — Decide Building Upgrade target/resource/queue policy
- Task ID: `REC-P06`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Define an explicit allowlist for any Daily Building Upgrade target, level transition, queue state, exact resource cost, and premium/box policy, while denying upgrades under unknown policy.
- Dependencies: none
- Related work: `DQ-FLOW-BUILDING-UPGRADE` (retain disabled generic identity/level arithmetic and Vehicle Depot Main exclusion); `TROOP-TRAINING-END-TO-END-CONSOLIDATION` (keep facility training distinct from building upgrade). The historical target/resource/queue gap is retained in the source policy record; no separate Building Upgrade gate task exists.
- Evidence: `tasks/building_upgrade_disabled.py` recognizes exact building/level/queue/cost facts and always blocks; product policy global record keeps upgrades policy-disabled and historical backlog says target/resource/queue decision is missing; `tests/test_building_upgrade_disabled.py` covers unknown cost/queue and Main-only negatives.
- Scope: `tasks/building_upgrade_disabled.py`; `tests/test_building_upgrade_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Product owner must list exact allowed building identities, current→target levels, queue occupancy rule, resource/currency and amount, quantity/cadence, free/box/premium rule, successor/Home, and denied targets (including Vehicle Depot Main boundary). Until complete, all upgrades deny. A future implementation ticket must detail route paths and atomic integration; policy alone cannot enable it.
- Non-goals: No upgrade input, automatic pack/box use, premium, strategic target inference, training, research, or registry/scheduler mutation.
- Acceptance: 1. Every allowlisted target has exact identity/level/queue/cost/resource and allowed/denied result. 2. Unknown or changed target/queue/cost blocks. 3. Vehicle Depot/Main-only and premium boundaries are explicit. 4. Policy result does not claim implementation/native readiness. 5. Future code ticket is mandatory; UNKNOWN never auto-retries.
- Verification: Existing `python -m unittest tests.test_building_upgrade_disabled` is prescribed after policy resolution; no command is run here.
- Native gate: none; later implementation requires current `REC-O10` target/resource/queue scenario.
- Integration owner: Product-policy owner edits only the Building Upgrade policy record; future implementation owner must own registry/handler/contract hunks.
- PR boundary: `recovery/rec-p06` — Building Upgrade policy decision.
- Rollback: Keep upgrades disabled and preserve Main-only exclusion; never restore generic upgrade dispatch.
- Runtime authorization: none.
- Completion: Policy-only resolution; no building upgrade is considered implemented or done.

### REC-P07 — Decide Tech Upgrade target/resource/queue policy
- Task ID: `REC-P07`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Define the exact technology target, prerequisite, level, queue, resource/cost, cadence, and premium/box policy for any Tech Upgrade; deny research while any field is unresolved.
- Dependencies: none
- Related work: `DQ-FLOW-TECH-UPGRADE` (retain disabled prerequisite/level arithmetic); `DQ-FLOW-DONATION` (keep research upgrade separate from Alliance donation).
- Evidence: `tasks/tech_upgrade_disabled.py` requires technology identity, prerequisites, level, queue, exact cost, current resource balance, and successor but always blocks; product policy marks upgrades policy-disabled and missing target/resource/queue decision; `tests/test_tech_upgrade_disabled.py` covers disabled and ambiguous paths.
- Scope: `tasks/tech_upgrade_disabled.py`; `tests/test_tech_upgrade_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Record exact allowlist or deny. An allowed contract must name technology, current/target level, prerequisites, queue state, resource and amount, quantity/cadence, free/box/premium rule, successor/Home, and all denied substitutes. No implementation ticket may infer these from OCR or stale policy. A separately detailed code ticket follows only after decision.
- Non-goals: No Research/Upgrade input, resource spending, automatic packs, premium, donation, or service registration/scheduler change.
- Acceptance: 1. Exact target/prerequisite/level/queue/resource/cost and allow/deny are recorded. 2. Unknown/changed state denies before dispatch. 3. Premium/box/substitute policy is explicit. 4. Decision is not implementation or native acceptance. 5. Future route ticket is mandatory and unknown effect never auto-retries.
- Verification: Existing `python -m unittest tests.test_tech_upgrade_disabled` is prescribed after policy update; no command is run here.
- Native gate: none; later code requires current `REC-O10` Tech Upgrade scenario.
- Integration owner: Product-policy owner updates only the Tech Upgrade record; no registry/handler/queue/scheduler hunk is allowed.
- PR boundary: `recovery/rec-p07` — Tech Upgrade policy decision.
- Rollback: Keep research upgrade disabled and preserve prerequisite/cost blockers; no legacy fallback.
- Runtime authorization: none.
- Completion: Policy disposition only; no Tech Upgrade action is implemented or done.

### REC-P08 — Decide Alliance Tech Donation 10 target/resource policy
- Task ID: `REC-P08`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Resolve the exact Alliance Technology target and per-donation resource/cost semantics for the “Donation10” obligation. The Daily obligation count is known to be exactly 10 donations; only the allowed resource, target, amount/cost, balance, and successor semantics remain unresolved before any donation can be allowed.
- Dependencies: none
- Related work: `DQ-FLOW-DONATION` (retain disabled donation arithmetic and resource guards); `DQ-FLOW-PURCHASES` (keep Donation separate from Shop purchase).
- Evidence: `tasks/donation_disabled.py` requires exact tech identity, resource identity/known status, amount, balance, count, successor, and Home but always blocks; product policy globally prohibits donations and backlog allocation requests exact target/resource; `tests/test_donation_disabled.py` covers disabled dispatch and mismatch.
- Scope: `tasks/donation_disabled.py`; `tests/test_donation_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Product owner must decide allow/deny, exact technology, allowed resource type, per-donation amount/cost and balance, exactly ten donation occurrences per reset, successor/Home, and premium/box prohibition. Until resolved all donation input is denied. If allowed, a future implementation ticket must specify target binding and atomic integration; policy does not authorize route work.
- Non-goals: No donation, resource use, purchase, tech upgrade, unbounded donation beyond the exact ten-count obligation, premium, or registry/scheduler mutation.
- Acceptance: 1. The Daily obligation is recorded as exactly 10 donation occurrences, never reinterpreted as a resource amount. 2. Each occurrence has an explicit technology target, allowed resource, amount/cost, balance, successor, and Home contract; any unknown or ambiguous per-donation resource/cost/target blocks. 3. Purchase, upgrade, and donation owners remain separate. 4. Policy record does not claim implementation/native proof. 5. Future code ticket is required and `UNKNOWN` never auto-retries.
- Verification: Existing `python -m unittest tests.test_donation_disabled` is prescribed after policy update; no command is run here.
- Native gate: none; later implementation requires current `REC-O10` Alliance Tech scenario with exact target/resource/count.
- Integration owner: Product-policy owner changes only the Donation policy record; no service registration, queue, handler, or scheduler update.
- PR boundary: `recovery/rec-p08` — Alliance Donation10 policy decision.
- Rollback: Keep donations denied and preserve resource/target ambiguity blocker; never restore broad donation.
- Runtime authorization: none.
- Completion: Policy-only result; no donation route is implemented or done.

### REC-P09 — Decide 180-minute Speedup policy
- Task ID: `REC-P09`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Resolve whether an exact 180-minute item-based Speedup may be used, naming timer target, item identity, quantity, and non-premium cost; deny all speedups while unresolved.
- Dependencies: none
- Related work: `DQ-FLOW-SPEEDUP` (retain disabled 180-minute item/timer arithmetic). The historical deferral is recorded in BACKLOG; no separate Speedup evidence-gate task exists.
- Evidence: `tasks/speedup_disabled.py` fixes 180 minutes/10800 seconds and exact timer/item guards but always blocks; product policy globally marks speedups policy-disabled; `tests/test_speedup_disabled.py` covers item/timer arithmetic and unknown blocking; BACKLOG explicitly defers Speedup 180 minutes.
- Scope: `tasks/speedup_disabled.py`; `tests/test_speedup_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Record allow/deny, exact timer target, item identity, quantity, duration arithmetic, inventory balance, free/premium policy, successor/Home, and per-reset cadence. Until recorded, no Speedup. If allowed, open a separately detailed implementation ticket; do not infer from a visible timer or stale item name.
- Non-goals: No speedup input, item consumption, premium currency, arbitrary timer, item waste, purchase, or scheduler/registry change.
- Acceptance: 1. Exact 180-minute item/timer/quantity and allow/deny are recorded. 2. Unknown item, target, cost, balance, or successor denies. 3. Premium and arbitrary-duration substitutions remain denied. 4. Policy does not claim implementation/native readiness. 5. Future route ticket is mandatory and UNKNOWN never auto-retries.
- Verification: Existing `python -m unittest tests.test_speedup_disabled` is prescribed after policy resolution; no command is run here.
- Native gate: none; later code requires current `REC-O10` Speedup scenario if policy allows it.
- Integration owner: Product-policy owner updates only the Speedup record; no runtime registry/handler/scheduler changes.
- PR boundary: `recovery/rec-p09` — Speedup policy decision.
- Rollback: Keep Speedup denied and preserve timer/item ambiguity; never restore item waste or premium fallback.
- Runtime authorization: none.
- Completion: Policy disposition only; no Speedup action is implemented or done.

### REC-P10 — Decide Resource Building Boost policy
- Task ID: `REC-P10`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Resolve whether a Resource Building Boost may occur, naming the exact building/resource, duration, quantity/cadence, cost, and no-premium boundary; keep boost separate from Buy Box policy.
- Dependencies: none
- Related work: `DQ-FLOW-RESOURCE-BOOST` (retain disabled any-building recognition and timer arithmetic); `DQ-FLOW-PURCHASES` (retain Buy Box as a separate logical outcome, not a boost alias).
- Evidence: `tasks/resource_boost_disabled.py` defines `RESOURCE_BUILDING_SCREEN`, exact boost target, resource types, duration/cost guards, postcondition replay, and always-block dispatch; `tests/test_resource_boost_disabled.py` covers unknown building, duration/cost, and disabled behavior; product policy/global premium record denies unapproved boosts and current target/duration/cost remain unresolved.
- Scope: `tasks/resource_boost_disabled.py`; `tests/test_resource_boost_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Product owner must choose allow/deny and exact building identity, resource type, boost duration, quantity/cadence, cost/resource, inventory/balance, successor/Home, and premium/refill policy. Until then boost remains denied. An allowed outcome requires a separate implementation ticket and cannot authorize Buy Box.
- Non-goals: No boost input, Buy Box, purchase, premium, uncontrolled duration, arbitrary building/resource, or registry/scheduler change.
- Acceptance: 1. Exact building/resource/duration/cost/quantity and allow/deny are recorded. 2. Unknown or changed building/cost/duration denies. 3. Premium, paid, and box substitutions remain denied unless separately and explicitly specified (default deny). 4. Decision does not claim implementation/native readiness. 5. Future code ticket is mandatory and unknown effect never auto-retries.
- Verification: Existing `python -m unittest tests.test_resource_boost_disabled` is prescribed after policy update; no command is run here.
- Native gate: none; any future allowed implementation needs current `REC-O10` boost scenario and exact timer/effect evidence.
- Integration owner: Product-policy owner updates only the Resource Boost record. No shared purchase/registry/handler mutation is permitted; a later implementation ticket must own exact boost route hunks.
- PR boundary: `recovery/rec-p10` — Resource Building Boost policy decision.
- Rollback: Keep boost disabled and preserve unknown target/cost blocker; never merge it with Buy Box.
- Runtime authorization: none.
- Completion: Boost policy only; Buy Box remains a distinct unresolved decision and no feature is done.

### REC-P11 — Defer Rare Earth Pit income discovery and policy
- Task ID: `REC-P11`
- Type: Decision
- Priority: P3
- Status: BLOCKED_POLICY
- Objective: Resolve whether Rare Earth Pit income collection is a permitted capability at all, including exact trigger, amount, cadence, target identity, and successor; until then treat the Pit as observation-only.
- Dependencies: none
- Related work: `DQ-FLOW-PURCHASES` (keep Rare Earth Shop distinct from Pit income). BACKLOG's explicit Rare Earth Pit deferral is retained, but it has no separate legacy task ID.
- Evidence: `tasks/home_atlas.py`/`tasks/assets/home_atlas/bluestacks/800x1280/additional_buildings.json` identify the semantic `home.building.pit`/Pit label only; no retained Pit income route or exact current income contract exists; `tasks/flow_delivery_product_policy.json` has no explicit income allowlist and global unknown-consequence policy fails closed. No current native income evidence is claimed.
- Scope: `tasks/home_atlas.py`; `tasks/assets/home_atlas/bluestacks/800x1280/additional_buildings.json`; `tasks/flow_delivery_product_policy.json`; `tests/test_home_atlas.py`
- Changes: Product owner must choose allow/deny and, if allowed, exact Pit target, readiness trigger, income resource/amount, cadence/reset, collection quantity, cost (must be zero or explicitly specified), successor/Home, and no-premium rule. Until complete, Pit recognition is navigation/observation only and no collection input is permitted. An allowed result requires a new implementation ticket with exact route/test/integration paths.
- Non-goals: No Pit tap/collection, Rare Earth Shop purchase, boost, resource spend, income inference from label, or registry/scheduler mutation.
- Acceptance: 1. Decision records exact allow/deny and all trigger/resource/amount/cadence/successor fields. 2. Pit label alone never authorizes income. 3. Unknown readiness/effect blocks and never retries. 4. Policy does not claim implementation/native readiness. 5. Future code ticket is separately detailed before any route work.
- Verification: Existing `python -m unittest tests.test_home_atlas` is prescribed after policy resolution; no command is run here.
- Native gate: none while deferred; an allowed future route requires a current `REC-O10` Pit scenario and cannot use atlas label as effect proof.
- Integration owner: Product-policy owner updates only the Pit disposition; no registry/handler/scheduler route is created by this ticket.
- PR boundary: `recovery/rec-p11` — Rare Earth Pit policy/discovery decision.
- Rollback: Preserve observation-only Pit state and explicit deferment; never restore an inferred income collector.
- Runtime authorization: none.
- Completion: Deferred or explicit policy outcome is recorded; no Pit income capability is implemented or done.

### REC-P12 — Decide Resource Buy Box policy separately from Boost
- Task ID: `REC-P12`
- Type: Decision
- Priority: P2
- Status: BLOCKED_POLICY
- Objective: Resolve whether the `buy_box` outcome may purchase a specific resource box, naming exact box/item, currency, quantity, cost, balance, and successor; never conflate this decision with REC-P10 Resource Building Boost.
- Dependencies: none
- Related work: `DQ-FLOW-PURCHASES` (retain disabled four-variant shop contract and buy-box identity); `DQ-FLOW-RESOURCE-BOOST` (retain Boost as a distinct policy outcome, not a shared completion).
- Evidence: `tasks/purchases_disabled.py` maps `buy_box` to a distinct shop/offer identity and always blocks dispatch; `tests/test_purchases_disabled.py` covers four-way shop separation, cost/currency arithmetic, and changed-offer blocking; BACKLOG says `buy_box` remains admitted as a historical policy candidate, but current product identity/cost/successor evidence is missing. Product/global policy prohibits unapproved purchases.
- Scope: `tasks/purchases_disabled.py`; `tests/test_purchases_disabled.py`; `tasks/flow_delivery_product_policy.json`
- Changes: Product owner must choose allow/deny and exact box/item identity, contents/resource quantities, currency, cost, sufficient balance, quantity/cadence, no-premium/refill rule, successor/Home, and whether any box may be used as a continuation for another route. Until resolved, Buy Box is denied. If allowed, open a separate implementation ticket with exact route/test/registry hunks; it must not modify REC-P10's Boost decision.
- Non-goals: No Buy input, resource-box consumption, Resource Building Boost, automatic offer selection, premium/purchase fallback, balance mutation, or registry/scheduler change.
- Acceptance: 1. Buy Box has an explicit allow/deny and exact box/item/contents/currency/cost/quantity contract. 2. Unknown or changed offer, cost, currency, balance, or successor denies. 3. Boost and Buy Box remain separate policy rows and future PRs. 4. Decision does not claim implementation/native readiness. 5. Future code ticket is mandatory and unknown purchase effect never auto-retries.
- Verification: Existing `python -m unittest tests.test_purchases_disabled` is prescribed after policy update; no command is run here.
- Native gate: none; later allowed implementation requires current `REC-O10` Buy Box evidence and exact resource/currency delta.
- Integration owner: Product-policy owner updates only the exact Buy Box record and does not touch REC-P10 or runtime registration. Future implementation owns exact `automation_service/registry.py`, `automation_service/handlers.py`, and route contract hunks.
- PR boundary: `recovery/rec-p12` — Buy Box policy decision.
- Rollback: Keep Buy Box denied and preserve separate Boost policy; never restore a broad purchase adapter.
- Runtime authorization: none.
- Completion: Buy Box policy disposition only; no purchase or boost feature is implemented or done.

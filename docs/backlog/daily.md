# Daily recovery tickets

Planning-only recovery records. Every ticket below is inactive: `READY` means eligible for a later offline implementation PR, while `WAITING_DEPENDENCIES` means its code prerequisites are incomplete and `BLOCKED_NATIVE` records a separate later evidence gate, not a reason to omit an offline implementation. Static `FlowSpec`/registry facts are non-authorizing and may bind a runner in the same atomic code PR; `default_enabled=false` only initializes missing SQLite rows and never enables an existing persisted row. The canonical SQLite `BotStateManager` is the sole mutable runtime authority and its flow rows remain disabled until the applicable acceptance. No catalog, queue/index, execution matrix, conductor, task-state, agent, registry projection, or evidence file authorizes dispatch. `tasks/daily_quest_state.py` may be read only as a historical source, reused as a pure classifier, or retired as an adapter; it is never live authority. Temporary fake/replay tests are allowed, but no ticket author may dispatch gameplay input, mutate actual game state, enable a registry row, or treat historical native evidence as current acceptance.

### REC-D01 — Reconcile current Daily catalog identity and no-premium policy
- Task ID: `REC-D01`
- Type: Decision
- Priority: P0
- Status: READY
- Objective: Establish one auditable current Daily identity and policy map for all 31 admitted catalog keys plus explicitly tracked non-catalog candidates. Resolve the known identity and policy contradictions before any route is dispatched: Daily `Join` for Ultimate is not Main `Clear`; Nova Praise is not Personal Might Praise; `use_resource_item` is not automatically Daily-owned; queued training or march return is not completion; and Supply Depot permission is free-only until Free disappears. The result is non-authorizing documentary/static authority, not gameplay behavior, a queue, or a runtime registry.
- Dependencies: `none`
- Related work: `DQ-CATALOG-RECONCILIATION` — retain its 31-key selected-Daily provenance and exclusions; `DQ-COVERAGE-MATRIX` — retain its one-owner and dormant-registration rules; `DQ-FOUNDATION-DAILY-INVENTORY` — reuse its selected-tab/current-reset identity boundary; `FLOW-PRODUCT-POLICY-RECONCILIATION-20260723` — retain as historical policy reconciliation, superseded only where this current map corrects stale wording; `MVP-QUEST-TO-CLAIM` — retain historical vertical-slice facts, never treat its legacy labels as current canonical acceptance.
- Evidence: `tasks/daily_quest_catalog.json` (catalog version 3, selected-Daily provenance and 31 admitted keys); `tasks/daily_quest_execution_matrix.json` (`portfolio_reconciliation`, one owner/state per key and non-catalog ownership); `docs/portfolio-requirements-inventory.md` (current identity and evidence ordering); `tasks/flow_delivery_product_policy.json` (free-only, zero-cost, recruitment, Ultimate, Supply, Bioenhancer, and Nova policy records); `tests/test_daily_quest_planning.py` and `tests/test_catalog_and_pnsctl.py` (offline catalog/ownership and attribution assertions). The prior audit found 80 isolated fake/temp canonical/state tests, not native acceptance; current native labels therefore remain evidence classification only.
- Scope: `tasks/daily_quest_catalog.json`; `tasks/daily_quest_execution_matrix.json`; `tasks/flow_delivery_product_policy.json`; `docs/portfolio-requirements-inventory.md`; `tests/test_daily_quest_planning.py`
- Changes: (1) Publish a table mapping every admitted key to exactly one recovery owner: `upgrade_building`→`REC-P06`, `join_hero_duel`→`REC-P05`, `upgrade_tech`→`REC-P07`, `train_fighter`→`REC-R06`, `train_rider`→`REC-R07`, `train_shooter`→`REC-R08`, `train_vehicle`→`REC-R09`, `recruit_noahs_tavern`→`REC-D07`, `upgrade_hero`→`REC-P04`, `defeat_zombie_lair` and `consume_stamina`→`REC-R19` (deferred pending its explicit policy), `consume_ap`→`REC-R11`, `help_allies`→`REC-D08`, `buy_box`→`REC-P12`, `gather_wood` and `gather_steel`→`REC-R17`, `gather_gas`→`REC-R18`, `boost_resource_building_output`→`REC-P10`, `ruins_shop_purchase`→`REC-P01`, `rare_earth_shop_purchase`→`REC-P02`, `alliance_shop_purchase`→`REC-P03`, `speedup_using_items`→`REC-P09`, `bioenhancer_research`→`REC-D06`, `craft_nanoweapon`→`REC-R15`, `personal_might_praise`→`REC-D09`, `enhance_chip`→`REC-R04`, `enhance_module`→`REC-R05`, `enhance_gear`→`REC-R03`, `donate_alliance_tech`→`REC-P08`, `supply_depot`→`REC-D10`, and `ruins_challenge`→`REC-R12`. Preserve each matrix state (`ACCEPTED_EXISTING`, `IMPLEMENTATION_AND_EVIDENCE_REQUIRED`, `EVIDENCE_REQUIRED`, or explicit deferral) beside the new owner.
(2) Add non-catalog reconciliation rows: Ultimate Daily `Join`→`REC-D05` and separate from Campaign AP `REC-R11`; `use_resource_item`→`REC-R02` as a candidate with no Daily ownership until its direct-resource and selected-Daily evidence contract is met; and Gather Food→`REC-R16` as a proving slice explicitly excluded from Daily admission. Record that `REC-P12` is the separate BuyBox policy, not `REC-P10` ResourceBuildingBoost.
(3) State that premium/cash purchases, paid or ambiguous controls, refill, 10x, and item-backed substitutes are universally forbidden; legitimate per-route resource/AP/stamina effects remain governed by their own R/P tickets and are not globally forbidden. Require a current positive postcondition for training/march completion, never a projected timer or queue state. Resolve Supply conflicts to positively recognized Free controls only, stop when Free disappears, and never infer Daily 5/5 from collection permissions.
- Non-goals: No route implementation, native capture, registration, scheduler eligibility, state migration, new queue/registry/index authority, or rewrite of historical evidence. This ticket does not declare any route accepted merely because a legacy task says Completed or LIVE_VALIDATED.
- Acceptance: (1) All 31 catalog keys appear once in the mapping with the exact owner above and no duplicate dispatch owner. (2) Ultimate Join, Main Clear, Nova, Personal Might, Resource Item, training/march, and Supply Free-only boundaries are explicit and machine-checkable in the authority records. (3) `buy_box` is mapped to `REC-P12`, while `REC-P10` remains ResourceBuildingBoost only; Gather Food is visibly non-Daily. (4) Every deferred or evidence-required row retains its historical owner, state, and missing proof without being promoted. (5) Offline negatives reject Main-only identity, projected completion, premium/paid controls, unknown cost, and Free-disappeared Supply states. (6) The resulting catalog/policy facts cannot claim, reserve, enable, or dispatch a runtime occurrence.
- Verification: Prescribe existing `python -m unittest tests.test_daily_quest_planning tests.test_catalog_and_pnsctl`; add no new test file unless an observable mapping boundary is not covered. Commands are for the future implementation PR and are not executed by this planning author.
- Native gate: none; this is a pure offline authority decision. No native route is enabled by merging it.
- Integration owner: Main owns the shared catalog/matrix/policy reconciliation as non-authorizing documentary/static facts. This ticket must not add or mutate a queue, legacy backlog index, mutable registry, scheduler, or task-state authority. Later code PRs consume the facts through static `automation_service/registry.py` `FlowSpec` bindings (`default_enabled=false` only initializes missing SQLite rows) and canonical `automation_service/state.py`; each route PR owns its exact `scripts/pnsctl.py` and caller cutover hunks, with no separate registration task or “wire later” path.
- PR boundary: One future offline authority PR, title `Reconcile current Daily catalog identity and policy`, branch `recovery/rec-d01`; no code or native PR in this ticket.
- Rollback: Revert only the new mapping/policy records and keep every legacy evidence record unchanged; disable any dependent row or global Daily gate rather than restoring a legacy dispatch bypass.
- Runtime authorization: none; zero transport, zero live/native observation, zero persistent task-state or evidence mutation.
- Completion: The versioned catalog/matrix/policy map and focused offline proof are reviewed, with all dependencies and deferred rows explicit. This is documentation/authority completion only; it does not confer route, scheduler, native, R3, R5, or R6 readiness.

### REC-D02 — Provide the canonical selected-Daily inventory and reset fact
- Task ID: `REC-D02`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Replace scattered selected-Daily observations with one current-frame fact provider whose reset-scoped durable facts are persisted through canonical SQLite `BotStateManager`. It returns selected-tab identity, bounded rows, objective progress/control state, and game-day/reset identity. Unknown, clipped, stale, Main, or contradictory inventory must fail closed before any route or Claim can be authorized; observation alone never completes an objective.
- Dependencies: `REC-D01`, `REC-F09`, `REC-F10`, `REC-F11`
- Related work: `DQ-FOUNDATION-DAILY-INVENTORY` — reuse its selected-Daily recognition, Main-negative, clipped-row abstention, and overlap rules; `M6-DQ-BOOTSTRAP` — retain captured bootstrap corpus as fixture evidence only; `MVP-QUEST-TO-CLAIM` — retain its historical selected-Daily transition evidence but supersede its fact-source ownership with this provider.
- Evidence: `tasks/daily_quest.py` (`RouteDispatcher`, current `AllianceHelpHandler` and `PersonalMightPraiseHandler` consumers); `scripts/daily_row_claim_bluestacks.py` (`run_daily_row_reconnaissance`, `run_quest_daily_continuation`); `automation_service/state.py` (`BotStateManager`); `tasks/daily_quest_state.py` (read-only reset/classification source or retired adapter, never authority); `tasks/daily_quest_catalog.json` (admitted identity); `docs/portfolio-requirements-inventory.md` (selected-Daily evidence ordering). Existing foundation evidence is navigation/recognition only and does not prove a current reset provider or permit dispatch.
- Scope: `tasks/daily_quest.py`; `scripts/daily_row_claim_bluestacks.py`; `automation_service/state.py`; `automation_service/registry.py`; `tests/test_daily_quest_planning.py`
- Changes: (1) Define one provider contract for selected Daily tab, profile/device/reset identity, bounded row identity, progress before/after, visible control class, reward/cost, and source/evidence provenance. (2) Reconcile scroll overlap by stable objective key and refuse clipped or duplicate rows; reject Main, synthetic, wrong-profile, unknown-reset, and contradictory overlays. (3) Make every future Daily consumer request this provider rather than reading an ad-hoc row or static screenshot; provider observation is authorization input only and never completion. (4) Persist only the reset identity/fact needed by later route state in canonical `BotStateManager` SQLite. `tasks/daily_quest_state.py` may remain a read-only source/pure classifier or be retired, but must not write or serve live state. (5) Do not add a queue, registry, index, or second recognizer.
- Non-goals: No consequential input, ordinary or milestone Claim, route registration, scheduler activation, catalog expansion, native evidence capture, or implementation of resource/shop/gathering policies.
- Acceptance: (1) A positive current selected-Daily frame yields one deduplicated bounded inventory with reset identity and exact row controls. (2) Main-only, synthetic, clipped, wrong-profile, stale-reset, duplicate-overlap, unknown-overlay, and missing-control inputs return an explicit non-authorizable outcome. (3) A reset change invalidates old progress facts without deleting protected historical evidence. (4) Existing Claim, milestone, and route consumers can obtain the same fact shape without a second recognizer. (5) Provider execution has no transport calls and cannot imply objective completion or Claim readiness. (6) Restart/reload reads the same reset identity from canonical SQLite rather than `tasks/daily_quest_state.py`.
- Verification: Prescribe existing `python -m unittest tests.test_daily_quest_planning tests.test_daily_quest_state`; extend an existing focused module only if the reset/overlap boundary lacks coverage. Commands are prescribed, not executed here.
- Native gate: none for this offline provider; all future route dispatch remains disabled until its own REC-O10 record.
- Integration owner: Main owns the shared fact schema in `automation_service/state.py` and its static `automation_service/registry.py` runner facts. The implementation PR must update `tasks/daily_quest.py`, `scripts/daily_row_claim_bluestacks.py`, `scripts/pnsctl.py`, `automation_service/service.py`, and `automation_service/registry.py` atomically where callers are changed, hard-disable duplicate legacy inventory bindings, and persist mutable facts only through `BotStateManager`; no `tasks/daily_quest_state.py` write path or “wire later” caller is acceptable. Static runner bindings may land here, while SQLite flow rows remain `enabled=false`.
- PR boundary: One focused offline provider PR, title `Add canonical selected-Daily reset facts`, branch `recovery/rec-d02`; native route work is separate.
- Rollback: Disable the provider consumer gate and preserve the last known fact/evidence fixtures in canonical SQLite; do not restore ad-hoc row reads, a task-state authority, or dispatch from stale inventory.
- Runtime authorization: none; zero transport and zero live/native observation.
- Completion: Provider contract, migrated offline callers, and focused replay proof establish one fail-closed selected-Daily/reset fact source. Code merge does not imply native or scheduler readiness.

### REC-D03 — Cut ordinary Daily Claim over to one aggregate owner
- Task ID: `REC-D03`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Make one aggregate ordinary Daily Claim the sole Claim owner for the current reset, while recording each distinct durable ready batch/occurrence independently. Objective completion, route attribution, Personal Might, Bioenhancer, Supply, and milestone state must never dispatch or imply a row-local Claim. A reset may therefore contain multiple later-ready batches, each with its own durable identity.
- Dependencies: `REC-D02`, `REC-F07`
- Related work: `DQ-CLAIM-DAILY` — supersede its stale row-local execution ownership with this aggregate cutover while retaining its exact free/zero/one-cost and historical Claim evidence; `GNB-PHASE-E-DAILY-CLAIMS-OFFLINE` — retain its offline aggregate contract and no-input boundary; `MVP-QUEST-TO-CLAIM` — retain the historical quest-to-Claim slice, not its legacy per-row interpretation.
- Evidence: `tasks/available_daily_claim.py` (`AvailableDailyClaimObservation`, authorization, transaction spec, postcondition, pure pulse); `scripts/daily_row_claim_bluestacks.py` (current run/canary/continuation entry points); `automation_service/state.py` (`BotStateManager` runs/actions and occurrence identity); `tests/test_available_daily_claim.py`; `tests/test_flow_delivery_daily_row_claim_bluestacks.py`. The current matrix already states one aggregate Claim owner and objective completion does not imply Claim readiness; `tasks/daily_quest_state.py` is only a historical/read-only classifier if reused.
- Scope: `tasks/available_daily_claim.py`; `scripts/daily_row_claim_bluestacks.py`; `automation_service/state.py`; `automation_service/registry.py`; `tests/test_available_daily_claim.py`
- Changes: (1) Authorize one current-frame ordinary free non-milestone Claim with positive target identity, cost, reset, stable durable ready-batch/occurrence key, and postcondition; the key is bound to the reset, aggregate control, and readiness occurrence/revision, not a volatile capture timestamp. (2) Persist each verified ready batch and attribution in canonical SQLite; authorize at most one dispatch for each distinct durable ready batch/occurrence within a reset, while allowing a later-ready batch in that same reset to dispatch after its own readiness is observed. (3) Reopen/restart must reuse the same occurrence key and deduplicate it; two different ready batches must not collapse into one reset-wide quota. (4) An observed completed row alone never authorizes Claim; only the exact aggregate control and its current-frame contract do. (5) Retire row-local Claim dispatch and objective-specific Claim callbacks; milestone Claim remains a separate owner. (6) Stop and reconcile on unchanged, unknown, overlay, wrong reset, paid/ambiguous, or unresolved result; never retry blindly.
- Non-goals: No milestone chest, objective route, native capture, scheduler enablement, task-row creation, or resource/currency spend. This ticket does not make a route completion fact valid merely by calling Claim.
- Acceptance: (1) At most one ordinary Claim dispatch is authorized for each distinct durable ready batch/occurrence within a reset—not at most one Claim for the entire reset—and later ready batches remain eligible. (2) Two successive ready batches in one reset produce two distinct durable occurrences; after restart/reload, replaying either batch produces no duplicate dispatch. (3) A completed objective without a visible aggregate Claim remains unclaimed, and a later-ready batch survives restart/reload within the same reset without being lost. (4) Main Claim, milestone chest, row-local controls, paid/unknown controls, wrong reset, unchanged result, and unresolved result are rejected or blocked. (5) Positive Claim postcondition requires the same durable ready batch/occurrence, reset, and selected-Daily source identity, plus a verified aggregate Claim-control transition/exhaustion and a positive observed points delta; missing either signal is unresolved/nonretryable and cannot produce claimed state or another dispatch. (6) Every old live row-Claim caller is removed or hard-disabled in the PR.
- Verification: Prescribe existing `python -m unittest tests.test_available_daily_claim tests.test_flow_delivery_daily_row_claim_bluestacks`; retain/update only behavior tests for per-occurrence aggregate cardinality, two successive ready batches in one reset, restart-safe deduplication, later-ready durability, and unresolved blocking. Commands are prescribed, not executed; use temporary fake/replay SQLite/adapters only.
- Native gate: none for the offline cutover; any future native Claim invocation requires the stable route-specific `REC-O10-D03` record, whose current evidence must prove the same ready-batch/reset/selected-Daily identity, the verified Claim-control transition, and positive points delta together, and remains disabled until accepted.
- Integration owner: Main owns Claim registry/schema mutation. The PR must include retirement or hard-disable hunks for `scripts/daily_claim_canary.py` and the old per-row path in `scripts/daily_row_claim_bluestacks.py`, plus exact shared static-runner changes in `scripts/pnsctl.py`, `automation_service/registry.py`, and `automation_service/service.py`; the aggregate owner is the only live caller, all mutable occurrence/run/action facts use `BotStateManager`, and no legacy shim, queue, index, task-state authority, or “wire later” path remains. The static runner binding and disabled SQLite row land atomically.
- PR boundary: One focused Claim cutover PR, title `Make Daily Claim aggregate and reset-durable`, branch `recovery/rec-d03`; no milestone or native PR.
- Rollback: Disable aggregate Claim eligibility and retain durable completion/attribution facts; never re-enable the retired row-local or Main Claim bypass.
- Runtime authorization: none; zero transport, zero live/native observation, and no production state mutation.
- Completion: Code PR completion means the aggregate offline contract, canonical SQLite reset/occurrence durability, per-ready-batch restart-safe deduplication, migrated callers, and focused tests prove the sole Claim owner and require both same-batch/reset selected-Daily continuity signals: a verified Claim-control transition and a positive points delta. Native Claim acceptance remains the separate `REC-O10-D03` record; code merge does not imply native or scheduler readiness.

### REC-D04 — Add the separate ready milestone chest controller
- Task ID: `REC-D04`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Provide a separate milestone-chest controller that authorizes only a visibly ready current-reset chest at its exact threshold. It must never tap a projected points position, ordinary row Claim, Main Claim, or locked/static chest; an ordinary Claim points increase is not chest success.
- Dependencies: `REC-D02`
- Related work: `DQ-CLAIM-MILESTONE` — retain its separate milestone identity, free-cost, current-day, and postcondition contract; `GNB-PHASE-E-MILESTONES-OFFLINE` — retain offline chest contract and missing-ready-evidence statement; `DQ-FOUNDATION-DAILY-INVENTORY` — reuse selected-Daily/reset identity but do not merge milestone and ordinary inventory owners.
- Evidence: `tasks/activity_milestones.py` (current milestone observation/authorization contract); `tasks/gameplay_flow_contracts/DAILY-MILESTONE-CLAIM-BLUESTACKS-INTEGRATION.json` (separate milestone flow contract); `automation_service/state.py` (`BotStateManager` reset/occurrence/action persistence); `automation_service/registry.py` (static runner binding); `tests/test_activity_milestones.py` (ready/locked/static/wrong-panel/cost/overlay/reset cases). No current milestone route adapter or fresh ready-chest native evidence was found; the implementation path is therefore explicitly new, statically bindable, and inactive by disabled SQLite row.
- Scope: `tasks/activity_milestones.py`; `scripts/daily_milestone_claim_bluestacks.py (NEW)`; `automation_service/state.py`; `automation_service/registry.py`; `tests/test_activity_milestones.py`
- Changes: (1) Bind threshold, chest identity, current reset, visible ready state, zero cost, and panel-local target in one observation. (2) Add a one-dispatch controller/adapter with a static `FlowSpec` runner binding that records a same-threshold/reset chest transition `ready→claimed/opened` or a reward receipt positively bound to that chest identity and chest state; a points increase alone is insufficient because ordinary Claim changes points. (3) Persist milestone occurrence/result independently in canonical SQLite from aggregate Claim, with later-ready thresholds durable within the reset. (4) Do not alias ordinary Claim, emit an objective-specific Claim callback, or require a separate mutable registration; `default_enabled=false` seeds the SQLite row until the native gate is accepted. (5) Stop on unchanged, unknown, overlay, wrong threshold/reset, or locked state.
- Non-goals: No ordinary Claim, projected-points tap, new points calculator, route-wide Daily routine, native capture, scheduler activation, or reinterpretation of historical milestone labels.
- Acceptance: (1) Only a current visibly ready exact-threshold chest with known zero cost authorizes one input. (2) Locked, static, wrong-panel, unknown-cost, projected, stale-reset, duplicate, unchanged, and unresolved observations never authorize input. (3) Success requires the same threshold/reset chest state transition `ready→claimed/opened` or a reward receipt positively bound to that chest identity and resulting chest state; a points increase without that chest transition is unresolved and cannot pass. (4) Milestone state is durable in canonical SQLite and independent from aggregate ordinary Claim. (5) No ordinary Claim alias or objective completion callback is emitted by the milestone controller. (6) The static runner may be installed in the same atomic code PR, but its SQLite row remains disabled and no separate mutable registration is required.
- Verification: Prescribe existing `python -m unittest tests.test_activity_milestones`; if the new adapter adds behavior not represented by that contract, add one focused `tests/test_daily_milestone_claim_bluestacks.py` (NEW) covering chest-state binding versus points-only negatives, not broad suite coverage. Commands are prescribed, not executed; fake/replay inputs only.
- Native gate: none for the offline controller. The future adapter requires `REC-O10-D04` current native source/immediate-before/successor evidence and remains disabled until that record is accepted.
- Integration owner: Main owns the separate milestone schema and static runner binding. The code PR must include exact shared updates in `automation_service/registry.py`, `automation_service/state.py`, `scripts/pnsctl.py`, and any current matrix-only milestone binding, removing or hard-disabling the old binding while preserving ordinary Claim; the `FlowSpec` is installed with `default_enabled=false`, and there is no second mutable registration or later “wire it” PR.
- PR boundary: One focused milestone controller/contract PR, title `Separate ready Daily milestone chest`, branch `recovery/rec-d04`; native acceptance is a later operational record.
- Rollback: Disable milestone dispatch and preserve threshold/reset observations; never fall back to ordinary Claim or projected coordinate taps.
- Runtime authorization: none; zero transport, zero live/native observation, zero protected evidence mutation.
- Completion: Code PR completion means the offline milestone contract, static runner binding, canonical SQLite independent persistence, adapter boundaries, and focused proof are complete. The later REC-O10 native record separately proves current ready-chest evidence; no points-only or ordinary-Claim result and no code merge confers native or scheduler acceptance.

### REC-D05 — Migrate Ultimate Daily Join to the zero-resource Flee route
- Task ID: `REC-D05`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Make the selected-Daily Ultimate `Join` objective a distinct zero-resource route that starts directly from canonical Home, performs the exact challenge path through Flee, returns to canonical Home, and reconciles Daily progress from `0/1` to `1/1` with the visible `+5`. Current-reset Daily facts and completion are observed through the canonical provider in separately provenance-bound frames; a Daily frame need not share the Flee source/target frame or precede the direct Home route. Main `Clear`, Campaign AP, and any projected challenge result are not substitutes.
- Dependencies: `REC-D01`, `REC-D02`, `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`
- Related work: `ULTIMATE-CHALLENGE-DAILY-BLUESTACKS-INTEGRATION` — retain exact historical route/action facts but supersede its legacy LIVE_VALIDATED readiness label for current canonical acceptance; `ULTIMATE-CHALLENGE-NATIVE-EVIDENCE-AND-CANARY` — retain as the native scenario/budget gate and do not repeat its non-repeatable Flee action; `CAMPAIGN-AND-ULTIMATE-CHALLENGE-FLOW-SCOPE-CORRECTION` — retain the separation from Campaign AP `REC-R11`.
- Evidence: `tasks/ultimate_challenge_daily.py` (zero-resource authorization/postcondition contract); `scripts/bluestacks_ultimate_challenge.py`; `scripts/flow_delivery_ultimate_challenge_bluestacks.py`; `tasks/gameplay_flow_contracts/ULTIMATE-CHALLENGE-DAILY-BLUESTACKS-INTEGRATION.json`; `tasks/daily_quest.py` (canonical Daily fact consumer); `tests/test_ultimate_challenge_daily.py` and `tests/test_bluestacks_ultimate_challenge.py`. Historical artifacts record a prior Ultimate attempt/Flee and terminal Home, but the contract requires a fresh native actual-action source/target binding and separately observed current-reset Daily reconciliation; old labels are not new acceptance.
- Scope: `tasks/ultimate_challenge_daily.py`; `scripts/bluestacks_ultimate_challenge.py`; `scripts/flow_delivery_ultimate_challenge_bluestacks.py`; `tasks/gameplay_flow_contracts/ULTIMATE-CHALLENGE-DAILY-BLUESTACKS-INTEGRATION.json`; `tests/test_ultimate_challenge_daily.py`
- Changes: (1) Start at canonical Home and bind the actual Ultimate challenge/Flee source and target from a fresh native frame; do not force selected-Daily entry before the direct Home route. (2) Observe selected-Daily `Join Ultimate` reset/progress facts separately through the D02 provider, when available, with capture/provenance binding; these facts do not authorize Flee. (3) Reuse canonical Home→Campaign→Ultimate→Challenge→Hero Lineup Challenge→Exit→Flee→Home composition, with bounded navigation and fresh source/target/successor observations. (4) Permit no AP, stamina, currency, item, or Main-Quest Clear input; Flee is the terminal objective action and must not be repeated. (5) After the positive Flee successor and canonical Home, independently re-enter/observe Daily and reconcile `1/1` and `+5`; if that observation is unavailable or mismatched, stop unresolved. (6) Persist occurrence/progress/result facts in `BotStateManager`; retire duplicate live entry points in the same cutover.
- Non-goals: No Campaign AP Auto Battle, Main Clear, repeat Flee, reward/chest farming, lineup experimentation, premium/resource fallback, or global Daily routine.
- Acceptance: (1) The route's immediate-before frame proves the fresh actual Ultimate/Flee source, exact target, and zero-resource controls; a separately provenance-bound current-reset Daily observation may be before or after it and is not required in that frame. (2) Each route transition is bounded and fail-closed on unknown overlay, wrong screen, stale target, or contradictory successor. (3) Exactly one Flee dispatch occurs and no AP/stamina/currency/item input occurs. (4) Only a positive current Flee successor followed by canonical Home plus a separate current-reset Daily `1/1`/`+5` observation can attribute completion; any unresolved result remains blocked. (5) Main Clear and Campaign AP observations cannot satisfy this objective.
- Verification: Prescribe existing `python -m unittest tests.test_ultimate_challenge_daily tests.test_bluestacks_ultimate_challenge`; native evidence is separately assigned and not run by this planning author.
- Native gate: `REC-O10-D05` (current production composition, one authorized Flee budget, immediate-before, postcondition, Daily reconciliation, terminal Home). Disabled until `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, and an explicit operator authorization are satisfied; historical Flee evidence is not current acceptance.
- Integration owner: Main owns the Ultimate static runner/caller cutover. The PR must atomically migrate `scripts/pnsctl.py` and `automation_service/registry.py`, seed the `FlowSpec` with `default_enabled=false`, use `BotStateManager` for occurrence/run/action state and D02 for Daily facts, remove or hard-disable old live bindings in `scripts/bluestacks_ultimate_challenge.py` and `scripts/flow_delivery_ultimate_challenge_bluestacks.py`, and preserve replay/offline artifacts; no duplicate Ultimate dispatcher or separate mutable registration remains.
- PR boundary: One focused Ultimate Daily route PR, title `Migrate Ultimate Daily zero-resource Flee`, branch `recovery/rec-d05`; native acceptance is a separate REC-O10 record.
- Rollback: Disable the Ultimate Daily route and retain the historical attempt ledger; never restore Main Clear, Campaign AP, or repeat-Flee fallback.
- Runtime authorization: none for coding; no current native input. A later operator needs explicit one-route budget and authorization.
- Completion: Code PR completion means the offline route contract, canonical provider integration, canonical SQLite durability, static runner binding, retired duplicate callers, and focused proof are complete. The later REC-O10 record separately accepts current native Flee/source/target and Daily reconciliation; code merge is not native, scheduler, R3, R5, or R6 acceptance.

### REC-D06 — Migrate Bioenhancer to one free Research action
- Task ID: `REC-D06`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Migrate the Bioenhancer Daily route to exactly one current zero-cost `Free Research 1x` action from canonical Home, prove its cooldown/result and selected-Daily `0/1`→`1/1` progression, and return Home. Current-reset Daily facts and completion are observed through the canonical provider in separate provenance-bound observations from the fresh native Research source/target; do not force selected-Daily entry before direct Home. Paid Research, `10x`, and the old Bliss evidence must never authorize current BlueStacks dispatch.
- Dependencies: `REC-D01`, `REC-D02`, `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`
- Related work: `DQ-FLOW-BIOENHANCER` — retain its implementation and historical same-day action facts but supersede its old current-acceptance label for the BlueStacks gate; `BIOENHANCER-GNBOTS-PARITY` — retain the finding that GnBots has no Bioenhancer routine; `BIOENHANCER-FREE-RESEARCH-BLUESTACKS-INTEGRATION` — retain its flow contract/development artifact, with unresolved/current evidence state explicitly carried forward.
- Evidence: `tasks/bioenhancer.py` (`BioenhancerObservation`, free-single authorization, transaction/postcondition); `tasks/daily_bioenhancer.py` (selected-Daily ownership/progress); `scripts/bioenhancer_free_research_canary.py`; `scripts/flow_delivery_bioenhancer_bluestacks.py`; `tasks/gameplay_flow_contracts/BIOENHANCER-FREE-RESEARCH-BLUESTACKS-INTEGRATION.json`; `automation_service/state.py` (`BotStateManager`); focused Bioenhancer tests. The retained Bliss result proves a historical zero-cost action only; later development artifacts include an unresolved current session and do not close the BlueStacks gate. D02 Daily facts and the fresh Research source/target are separate bindings.
- Scope: `tasks/bioenhancer.py`; `tasks/daily_bioenhancer.py`; `scripts/flow_delivery_bioenhancer_bluestacks.py`; `tasks/gameplay_flow_contracts/BIOENHANCER-FREE-RESEARCH-BLUESTACKS-INTEGRATION.json`; `tests/test_flow_delivery_bioenhancer_bluestacks.py`
- Changes: (1) From canonical Home, bind exact current Research screen, fresh actual `Free Research 1x` source/target, quantity one, zero cost, and positive target provenance; do not require selected-Daily entry first. (2) Observe selected-Daily Bioenhancer reset/progress `0/1` separately through D02, before or after the route, and never use it to bind the Research target. (3) Dispatch one action, prove cooldown/result successor, then independently observe Daily `1/1` and return Home; missing/mismatched Daily observation remains unresolved. (4) Reject paid/10x/ambiguous/unknown cost, stale reset, result unresolved, or any duplicate action. (5) Keep ordinary aggregate Claim separate and persist occurrence/progress/result through `BotStateManager`; retire stale live canary/adapter callers in the same cutover while preserving historical evidence.
- Non-goals: No Research 10x, paid action, resource refill, Nova Praise, objective-specific Claim, repeat of a non-repeatable historical transaction, or scheduler/production activation.
- Acceptance: (1) Immediate-before evidence binds the fresh actual Research source/target, current Free Research 1x, quantity one, and zero cost; a separately provenance-bound current-reset Daily `0/1` observation is not required in that frame. (2) Exactly one free Research input is permitted per reset/cooldown. (3) Positive cooldown/result successor and canonical Home, followed by a separate current-reset Daily `1/1` observation, are required before attribution. (4) Paid/10x/unknown/unchanged/overlay/unresolved outcomes remain blocked without retry. (5) Claim remains aggregate-only, mutable facts use canonical SQLite, and the static runner row remains disabled until the separate native gate.
- Verification: Prescribe existing `python -m unittest tests.test_daily_bioenhancer tests.test_bioenhancer tests.test_flow_delivery_bioenhancer_bluestacks`; native replay is not executed by this planning author.
- Native gate: `REC-O10-D06` (current BlueStacks source, fresh immediate-before Free Research 1x source/target, one dispatch, cooldown/result, separately observed current-reset Daily progress, terminal Home). Disabled until prerequisites and explicit supervised authorization; Bliss artifacts remain historical/non-accepting.
- Integration owner: Main owns the Bioenhancer static runner/caller cutover. The PR must update `scripts/pnsctl.py`, `automation_service/registry.py`, `automation_service/state.py`, and callers atomically, seed `FlowSpec(default_enabled=false)`, remove or hard-disable `scripts/bioenhancer_free_research_canary.py` and stale duplicate flow-delivery callers as live entry points, and keep mutable occurrence/progress state in `BotStateManager`; native evidence gates dispatch but is not a second registration gate.
- PR boundary: One focused Bioenhancer route migration PR, title `Migrate Bioenhancer free Research route`, branch `recovery/rec-d06`; native proof is separate.
- Rollback: Disable Bioenhancer dispatch and preserve historical result/session artifacts; never restore paid/10x or stale-Bliss fallback.
- Runtime authorization: none for coding; no current native input or evidence mutation. Later native validation requires explicit route budget/authorization.
- Completion: Code PR completion means offline migration, canonical SQLite occurrence/progress durability, static runner binding, caller retirement, and focused proof exist. The later current REC-O10 evidence separately accepts native Research source/target, successor, and Daily reconciliation; merge does not imply scheduler or production enablement.

### REC-D07 — Unify Daily recruitment and three-tier maintenance
- Task ID: `REC-D07`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Use one recruitment controller/dispatcher for both the selected-Daily Basic objective (five free singles, one per exact 600-second window) and independent Basic/Intermediate/Advanced free maintenance (Basic 600 seconds, Intermediate 86400 seconds, Advanced 172800 seconds). Persist each tier independently, release while waiting, return Home after every pass, and reject paid, premium, item-backed, 10x, or ambiguous recruitment.
- Dependencies: `REC-D01`, `REC-D02`, `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`
- Related work: `DQ-FLOW-RECRUITMENT` — retain its historical five-window Basic and independent-tier facts, but require current canonical replay under this ticket; `RECRUITMENT-BLUESTACKS-INTEGRATION` — reuse its Daily ownership contract; `RECRUITMENT-FREE-ATTEMPT-MAINTENANCE` — reuse its durable three-tier maintenance policy and native evidence while not treating legacy Completed as current acceptance; `NOAHS-TAVERN-HOME-ATLAS-MIGRATION` — retain canonical Tavern navigation and safe return.
- Evidence: `tasks/daily_recruitment.py`; `tasks/free_recruitment.py`; `tasks/noahs_tavern_recruit.py`; `tasks/noahs_tavern_recruit_maintenance.py`; `tasks/noahs_tavern_recruit_runtime.py`; `scripts/noahs_tavern_recruit_bluestacks.py`; `scripts/flow_delivery_recruitment_bluestacks.py`; `tasks/flow_delivery_product_policy.json`; `tests/test_daily_recruitment.py`, `tests/test_free_recruitment.py`, `tests/test_noahs_tavern_recruit_maintenance.py`, and `tests/test_flow_delivery_recruitment_bluestacks.py`. Retained `.local-captures/flow-delivery/RECRUITMENT-FREE-ATTEMPT-MAINTENANCE/live/` proves historical production-controller sessions (Basic five windows plus Int./Advanced), but the current canonical acceptance gap remains explicit.
- Scope: `tasks/daily_recruitment.py`; `tasks/free_recruitment.py`; `tasks/noahs_tavern_recruit_maintenance.py`; `tasks/noahs_tavern_recruit_runtime.py`; `scripts/flow_delivery_recruitment_bluestacks.py`
- Changes: (1) Define one dispatcher that independently inspects Basic, Int., and Advanced eligibility and persists per-tier next-due/cooldown/reset state in canonical `BotStateManager` SQLite. (2) For Daily Basic, allow exactly five zero-cost singles in the reset, never a sixth; each action requires its own current frame and 600-second window. (3) For maintenance, allow at most one currently eligible free single per tier per pass, with Int. 86400-second and Advanced 172800-second cooldowns; cooling-down/exhausted tabs defer without blocking other tiers. (4) Release lease/state while waiting—including the 600-second Basic window—and require canonical Home after every pass/action. (5) Retire duplicate Basic/maintenance callers and install one static runner binding in the same atomic cutover; the SQLite row remains disabled until the separate native gate.
- Non-goals: No paid/premium/10x/item-backed recruitment, Claim input, queue projection as completion, shared cooldown across tiers, extra Basic attempts, or new scheduler/registry enablement.
- Acceptance: (1) Basic Daily reaches at most `5/5` through five separately authorized free singles and cannot dispatch a sixth in the reset. (2) Basic, Int., and Advanced due/cooldown state is independent and survives restart/reset; Int. and Advanced use 86400/172800 seconds exactly. (3) A cooling-down or exhausted tier defers while another eligible tier can proceed; waiting releases ownership. (4) Every successful pass returns canonical Home and proves result closure; unknown cost, overlay, unchanged result, or unresolved result stops that tier without retry. (5) No paid, premium, 10x, item-backed, or Claim input is possible, and only one dispatcher remains live.
- Verification: Prescribe existing `python -m unittest tests.test_daily_recruitment tests.test_free_recruitment tests.test_noahs_tavern_recruit_maintenance tests.test_flow_delivery_recruitment_bluestacks`; no test command is run by this planning author.
- Native gate: `REC-O10-D07A` (Basic five) and `REC-O10-D07B` (tier maintenance) (current production-controller evidence, independent windows/cooldowns, result closure, Home terminal, and prohibited-input audit). Disabled until prerequisites and explicit operator authorization; historical five-window evidence is retained but not silently promoted.
- Integration owner: Main owns recruitment static runner and canonical SQLite state schema. The PR must atomically migrate `scripts/pnsctl.py` and `automation_service/registry.py`, seed `FlowSpec(default_enabled=false)`, use `automation_service/state.py:BotStateManager` for all tier/occurrence state, hard-disable duplicate live callers in `scripts/flow_delivery_recruitment_bluestacks.py` and `scripts/noahs_tavern_recruit_bluestacks.py`, and route both Daily and maintenance through one dispatcher; no duplicate queue, mutable registry, or “wire later” registration remains.
- PR boundary: One focused recruitment-controller PR, title `Unify free recruitment Daily and maintenance`, branch `recovery/rec-d07`; current native proof is separate REC-O10 work.
- Rollback: Disable recruitment dispatch while retaining per-tier cooldown/reset ledger; never restore independent duplicate callers or a paid fallback.
- Runtime authorization: none for coding; no current native input. Later operator validation requires explicit Basic/tier budgets and authorization.
- Completion: Code PR completion means one offline controller, canonical SQLite independent tier state, retired callers, focused proof, and static runner binding are complete. The separate REC-O10 records later accept current native Basic five-window and tier-maintenance evidence; retained historical five-window facts remain reusable context but do not silently become current acceptance.

### REC-D08 — Implement native Alliance Help for ten free requests
- Task ID: `REC-D08`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Replace attribution-only `help_allies` handling with a native Alliance Help route that completes ten positively identified free Help requests (prefer Help All, otherwise one Help), proves each request/result successor, and keeps objective attribution separate from Claim.
- Dependencies: `REC-D01`, `REC-D02`, `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`
- Related work: `DQ-FLOW-ALLIANCE-HELP` — retain its zero-cost Help/Help All semantic contract and historical evidence, but supersede attribution-only ownership with a native route; `MVP-QUEST-TO-CLAIM` — retain historical Help evidence and unresolved Claim separation, not as current native acceptance; `DQ-CLAIM-DAILY` — retain aggregate Claim as the only Claim owner.
- Evidence: `tasks/daily_quest.py` (`AllianceHelpHandler`, currently attribution-oriented); selected-Daily/Alliance Help fixtures under `assets/navigation/800x1280/manifest.json` and their referenced evidence; `tasks/flow_delivery_product_policy.json` (zero-cost Help boundary); `tests/test_catalog_and_pnsctl.py` (existing offline Help attribution/catalog coverage). No current `scripts/alliance_help_bluestacks.py` or native route contract exists; both are explicitly NEW and must remain inactive.
- Scope: `tasks/daily_quest.py`; `tasks/gameplay_flow_contracts/ALLIANCE-HELP-BLUESTACKS-INTEGRATION.json (NEW)`; `scripts/alliance_help_bluestacks.py (NEW)`; `tests/test_alliance_help_bluestacks.py (NEW)`
- Changes: (1) Add a current-frame route contract from the canonical Daily fact/Home boundary to Alliance Help, binding exact free Help All target first and individual Help fallback only when positively identified. (2) Dispatch one request at a time, prove request/control/result change, count exactly ten successful free requests, and stop on no request, unknown target/cost, overlay, unchanged, or unresolved state. (3) Record `help_allies` attribution only after native result in canonical `BotStateManager`; never dispatch Claim. (4) Retire or hard-disable `AllianceHelpHandler` as a live route owner while retaining offline attribution/history, and install its static runner binding atomically with the SQLite row disabled.
- Non-goals: No Alliance Tech donation, purchase, Claim, generic popup, paid/unknown request, blind repeated taps, or inferred progress from a static Help screen.
- Acceptance: (1) A fresh actual Help source/target observation, separately from the D02 current-reset Daily facts, binds free Help All or one free Help with exact target identity. (2) Ten and only ten positive request/result successors are recorded; no input occurs after ten or after Free/Help disappears. (3) Unknown, ambiguous, paid, overlay, unchanged, wrong-reset, or unresolved states stop without retry. (4) Objective attribution is emitted only from the proven native result and canonical SQLite occurrence, and never authorizes ordinary/milestone Claim. (5) Static runner binding may land in the same code PR with its SQLite row disabled; no separate mutable registration or evidence-dependent registration gate exists.
- Verification: Existing `python -m unittest tests.test_catalog_and_pnsctl` covers retained offline attribution; prescribe NEW `python -m unittest tests.test_alliance_help_bluestacks` for the native route contract. Commands are prescribed, not executed.
- Native gate: `REC-O10-D08` (current production source, immediate-before target, ten bounded free requests, positive successors, stop boundary, terminal Home, and prohibited-input audit). Disabled until prerequisites and explicit authorization.
- Integration owner: Main owns static runner binding and objective attribution schema. The PR must update `scripts/pnsctl.py`, `automation_service/registry.py`, and `automation_service/state.py` atomically, seed `FlowSpec(default_enabled=false)`, remove/hard-disable the old live `AllianceHelpHandler` route binding in `tasks/daily_quest.py`, and preserve only offline/history attribution; no caller may remain “to wire later.”
- PR boundary: One focused native-route code PR, title `Add native Alliance Help Daily route`, branch `recovery/rec-d08`; REC-O10 validation is separate.
- Rollback: Disable native Help dispatch and retain proven attribution records; never restore attribution-only success or blind tap fallback as completion.
- Runtime authorization: none for coding; zero native input now. Later supervised operator needs explicit ten-request budget and authorization.
- Completion: Code PR completion means the offline route contract, canonical SQLite attribution/occurrence persistence, static runner binding, caller retirement, and focused proof are complete. The separate REC-O10 record later accepts current native Help evidence; code merge does not enable the disabled SQLite row or imply production readiness.

### REC-D09 — Implement Personal Might Praise separately from Nova
- Task ID: `REC-D09`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Implement one free native Praise for the exact Personal Might rank-one target, reconcile `personal_might_praise` only from its positive successor, and return Home without conflating the action with Nova Praise or dispatching Claim.
- Dependencies: `REC-D01`, `REC-D02`, `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`
- Related work: `DQ-FLOW-PERSONAL-MIGHT-PRAISE` — retain its exact target/zero-cost/offline contract but supersede attribution-only execution ownership with this native route; `PERSONAL-MIGHT-PRAISE-BLISS-PILOT` — retain historical Bliss evidence as non-current proof; `DQ-CLAIM-DAILY` — retain aggregate Claim separation; `GF-NOVA-PRAISE-SUPERVISED-20260722` — retain as Nova-only historical evidence and explicitly not a Personal Might substitute.
- Evidence: `tasks/daily_quest.py` (`PersonalMightPraiseHandler`, currently attribution-oriented); `tasks/gameplay_flow_contracts/PERSONAL-MIGHT-PRAISE-BLISS-PILOT.json`; `assets/navigation/800x1280/manifest.json` (Personal Might target fixtures); `tests/test_personal_might_praise.py` (offline identity/target/Claim separation). No current `tasks/personal_might_praise.py`, route contract, or `scripts/personal_might_praise_bluestacks.py` exists; proposed paths are explicitly NEW.
- Scope: `tasks/daily_quest.py`; `tasks/gameplay_flow_contracts/PERSONAL-MIGHT-PRAISE-BLUESTACKS-INTEGRATION.json (NEW)`; `scripts/personal_might_praise_bluestacks.py (NEW)`; `tests/test_personal_might_praise_bluestacks.py (NEW)`
- Changes: (1) Bind current Personal Might leaderboard/rank-one gold Praise target and zero-cost one-action attempt from a fresh actual target frame; observe selected-Daily identity/reset facts separately through D02 rather than requiring them in the action frame. (2) Dispatch exactly one Praise, require count/target successor and canonical Home, then attribute the Daily objective through canonical SQLite `BotStateManager`. (3) Stop on Nova screen, wrong rank, unknown cost, stale target, overlay, unchanged, cooldown/attempt ambiguity, or unresolved result. (4) Retire/hard-disable attribution-only handler as a live route owner, preserve offline historical records, keep Nova’s separate controller untouched, and install a disabled static runner binding in the same atomic cutover.
- Non-goals: No Nova Praise, repeat/cooldown farming, Claim, ranking navigation by static geometry, paid action, unknown target, or generic leaderboard rewrite.
- Acceptance: (1) Immediate-before frame proves Personal Might identity, exact rank-one target, and free control; separately provenance-bound selected-Daily facts may be observed before or after. (2) Exactly one native Praise occurs and positive target/count successor is required before attribution. (3) Nova identity, Main Claim, unknown/paid/unchanged/overlay/unresolved states never authorize success or retry. (4) Route returns to canonical Home and leaves aggregate/milestone Claim untouched. (5) Static runner binding is included in the code PR with its SQLite row disabled; no separate mutable registration or evidence-dependent registration gate exists.
- Verification: Existing `python -m unittest tests.test_personal_might_praise` covers the retained offline contract; prescribe NEW `python -m unittest tests.test_personal_might_praise_bluestacks` for route behavior. Commands are prescribed, not executed.
- Native gate: `REC-O10-D09` (current source, exact rank-one target, one free dispatch, positive successor, selected-Daily reconciliation, terminal Home, and Nova-separation audit). Disabled until prerequisites and explicit authorization.
- Integration owner: Main owns the Personal Might static runner/attribution cutover. The PR must update `scripts/pnsctl.py`, `automation_service/registry.py`, and `automation_service/state.py`, seed `FlowSpec(default_enabled=false)`, remove/hard-disable `tasks/daily_quest.py:PersonalMightPraiseHandler` as the live native owner, and ensure no Nova alias or duplicate caller remains; all integration hunks land in this PR.
- PR boundary: One focused Personal Might route PR, title `Add native Personal Might Praise route`, branch `recovery/rec-d09`; REC-O10 validation is separate.
- Rollback: Disable Personal Might dispatch and preserve historical evidence/attribution; never substitute Nova or restore an attribution-only completion path.
- Runtime authorization: none for coding; zero live/native input now. Later supervised validation requires explicit one-Praise budget and authorization.
- Completion: Code PR completion means the native-route code contract, canonical SQLite attribution/occurrence state, static runner binding, caller retirement, and focused proof are complete. The separate REC-O10 record later accepts current native Personal Might evidence; code merge does not imply Nova, scheduler, or production readiness.

### REC-D10 — Exhaust only free Supply Depot collections and reconcile Daily progress
- Task ID: `REC-D10`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Run the existing bounded Supply Depot Free-hold route against a positively recognized current Free Food control, exhausting exactly the observed free-attempt count in the supported `1..10` range before any paid/unknown action, while independently observing selected-Daily `supply_depot` progress before and after within the current reset. Claim Daily completion only when the actual observed successor is `5/5`; if positive free exhaustion leaves progress partial or advances it without reaching `5/5`, record successful bounded maintenance and defer Daily completion rather than forcing paid/unknown input. Collection count/effect and Daily completion are separate observations; neither may be projected from the other.
- Dependencies: `REC-D01`, `REC-D02`, `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`, `REC-R01`
- Related work: `DQ-FLOW-SUPPLY-DEPOT` — retain its selected-Daily/navigation and free-target contract, superseding evidence-only status with this current all-free sequence; `SUPPLY-DEPOT-VERIFIED-ROUTE-SEAM-CLOSURE` — retain canonical Home Atlas/target-binding seam facts; `SUPPLY-DEPOT-BLUESTACKS-INTEGRATION` — retain route artifacts but require current Free-disappearance and independent Daily reconciliation; `TOOLS-HOME-BASE-ATLAS-BLUESTACKS` — reuse canonical Home/Base navigation; `SUPPLY-DEPOT-LEGACY-ADAPTER-RETIREMENT` — retain completed legacy retirement and do not revive its blind/fixed collection path.
- Evidence: `tasks/supply_depot.py` (`SupplyDepotHoldConfig`, exact free reward/cost/quantity and bounded-hold postcondition); `tasks/daily_supply_depot.py`; `scripts/supply_depot_free_canary.py` (`select_free_food_control`, `exhausted_free_state`, and existing bounded `run` route); `scripts/flow_delivery_supply_depot_bluestacks.py` (`run_supply_depot` invokes the canary at lines 277-284, with `MAX_HOLD_TRANSPORT_CALLS=1` and retained continuous-trace counts); `tasks/gameplay_flow_contracts/SUPPLY-DEPOT-BLUESTACKS-INTEGRATION.json`; `tasks/flow_delivery_product_policy.json` (Free-only and never currency); `tests/test_daily_supply_depot.py`, `tests/test_supply_depot.py`, and `tests/test_flow_delivery_supply_depot_bluestacks.py`. Existing evidence proves navigation, Free-target recognition, bounded-hold/exhaustion semantics, and wrapper safety/count checks, but no current full fresh-frame production-path fake replay plus independent Daily `5/5` proof or later native acceptance.
- Scope: `tasks/supply_depot.py`; `tasks/daily_supply_depot.py`; `scripts/supply_depot_free_canary.py`; `scripts/flow_delivery_supply_depot_bluestacks.py`; `tests/test_flow_delivery_supply_depot_bluestacks.py`
- Changes: (1) Reuse the existing canary's Home/Atlas→Supply Depot navigation and current-frame binders; preserve the exact recognized Free Food target, quantity-one collection semantics, known zero cost, observed free-attempt count bounded to `1..10`, and `ResourceEffectAuthority` effect expectation. (2) Preserve the tested bounded Free hold gesture: revalidate the exact target and unchanged observed attempt count from a fresh immediate-before frame, derive `SupplyDepotHoldConfig` duration from that count, and make at most the wrapper's `MAX_HOLD_TRANSPORT_CALLS=1`; do not replace it with a speculative one-tap-per-item loop. Require the positive Free-disappearance/exhaustion successor (`free_attempts_after == 0` and no zero-cost controls), then use the visible facility Back arrow and verify terminal Home. Paid/premium spillover, unknown reward/cost/attempts, overlay, stale target, unchanged, or unresolved state blocks without input. (3) Independently observe selected Daily inventory through D02 with actual current-reset progress before and after; only an actual observed after-state of `5/5` may satisfy Daily completion. Positive free exhaustion with a reconciled resource effect and partial or below-`5/5` Daily progress is successful bounded maintenance with Daily deferred, not `resource UNKNOWN` or mandatory failure; collection permission/count/effect alone cannot substitute for Daily progress. (4) Persist exact observed count, effect/reconciliation, occurrence, and Daily facts through canonical `BotStateManager`, retire or hard-disable only conflicting blind/fixed/triple-tap/shortcut callers, and keep the static runner disabled; do not rebuild this existing route without a diagnosed current-composition gap.
- Non-goals: No currency/premium collection, fixed “four” or “five” assumption, Claim, other building collection, projected Daily completion, or resource mutation without R01 parity.
- Acceptance: (1) A full fresh-frame production-path fake replay exercises `scripts/flow_delivery_supply_depot_bluestacks.py` into the existing `scripts/supply_depot_free_canary.py` route and dispatches at most one bounded Free hold (`MAX_HOLD_TRANSPORT_CALLS=1`) only after exact target, quantity-one, known-zero-cost, and observed free-count validation for a count in `1..10`; its positive successor proves exact exhaustion (`free_attempts_after == 0` and Free disappeared). (2) No input occurs after Free disappears or on paid/premium/unknown/overlay/stale/unchanged/unresolved state; no paid spillover, fixed-count assumption, triple tap, or speculative one-tap-per-item loop is permitted. (3) Resource deltas/effects reconcile through `REC-R01` without guessed quantities, and occurrence state is canonical SQLite. (4) Separate current-reset selected-Daily observations record the actual progress before and after. Only an observed after-state of `5/5` may claim Daily completion; partial or below-`5/5` progress with positive free exhaustion/effect is successful bounded maintenance with Daily deferred, not `resource UNKNOWN` or mandatory failure, and no paid/unknown retry may force `5/5`. (5) Conflicting blind/fixed/shortcut live bindings are retired or hard-disabled without rebuilding the existing route absent a diagnosed composition gap; the static runner exists but its SQLite row remains disabled until the separate native gate.
- Verification: Prescribe existing `python -m unittest tests.test_daily_supply_depot tests.test_supply_depot tests.test_flow_delivery_supply_depot_bluestacks`; the implementation PR must also pass a full fresh-frame production-path fake replay through the wrapper's actual canary invocation, covering bounded hold-count enforcement for every supported `1..10` free-attempt count, exact free-count/exhaustion successor, effect reconciliation, independent current-reset Daily before/after classification for observed `5/5` and legitimate partial/deferred outcomes, and unknown/paid/unchanged blocking. No native command is executed by this planning author.
- Native gate: `REC-O10-D10` (later current native acceptance through the same production path: fresh source/immediate-before/result/terminal-Home frames, the single bounded Free hold and exact observed-count exhaustion for the recognized `1..10` count, R01 effect delta, and independent current-reset Daily progress before/after). Record Daily completion only when the actual observed after-state is `5/5`; a positive free-exhaustion/effect result with partial or below-`5/5` progress is accepted as bounded maintenance with Daily deferred, never as `resource UNKNOWN` or a reason for paid/unknown retry. No paid spillover or currency audit violation is accepted. Disabled until `REC-R01`, all prerequisites, full fresh-frame production-path fake replay, and explicit operator authorization; historical route artifacts are not current acceptance.
- Integration owner: Main owns Supply/ResourceEffectAuthority and static runner cutover. The PR must update `scripts/pnsctl.py`, `automation_service/registry.py`, `automation_service/state.py`, and `tasks/flow_delivery_product_policy.json` caller bindings atomically; seed `FlowSpec(default_enabled=false)`, preserve and integrate the existing `scripts/supply_depot_free_canary.py` route through `scripts/flow_delivery_supply_depot_bluestacks.py:277-284` with `MAX_HOLD_TRANSPORT_CALLS=1` and its fresh-frame/safety checks, and retire or hard-disable only conflicting blind/fixed/shortcut callers. Do not rebuild or duplicate the existing route without a diagnosed current-composition gap; all mutable occurrence/effect/reconciliation state remains in canonical SQLite, with no separate registration or evidence-dependent gate.
- PR boundary: One focused Supply Depot route PR, title `Exhaust free Supply Depot and reconcile Daily`, branch `recovery/rec-d10`; R01 parity and REC-O10 evidence are explicit prerequisites/gates.
- Rollback: Disable collection dispatch and retain observed effects/evidence; never restore blind fixed-count or paid fallback.
- Runtime authorization: none for coding; zero current native input. Later operator requires explicit all-Free budget and resource-effect authorization.
- Completion: Code PR completion means the existing canary/wrapper route is integrated without policy-changing gesture substitution, canonical SQLite occurrence/effect handling and R01-compatible reconciliation are complete, independent current-reset Daily before/after observation is wired with truthful `5/5` completion versus partial/deferred classification, the full fresh-frame production-path fake replay passes, conflicting callers are retired, and the static runner remains disabled. The separate `REC-O10-D10` record later accepts current native Free-exhaustion evidence; merge alone does not enable production or imply scheduler/native acceptance.

### REC-D11 — Canonicalize Nova to one free Praise pulse
- Task ID: `REC-D11`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Canonicalize Nova as one free Praise pulse from Home through Research Lab, preserving its attempt decrement/cooldown successor (fixed 300-second cooldown), bounded return Home, and no Personal Might/Daily conflation.
- Dependencies: `REC-F09`, `REC-F10`, `REC-F11`, `REC-F12`
- Related work: `NOVA-PRAISE-HOME-ATLAS-MIGRATION` — retain canonical Home Atlas navigation facts and historical readiness gaps; `NOVA-PRAISE-SUPERVISED-ONE-FREE-PULSE` — retain one-free-pulse contract and prior native records, but require the current REC-O10 scenario; `GF-NOVA-PRAISE-SUPERVISED-20260722` — retain historical supervised Nova result only; `PERSONAL-MIGHT-PRAISE-BLISS-PILOT` — retain as a negative identity boundary, never a Nova implementation.
- Evidence: `tasks/nova_praise.py`; `tasks/nova_praise_pulse.py`; `tasks/nova_praise_runtime.py`; `scripts/nova_praise_bluestacks.py`; `scripts/flow_delivery_nova_praise_bluestacks.py`; `tasks/gameplay_flow_contracts/NOVA-PRAISE-SUPERVISED-ONE-FREE-PULSE.json`; `tests/test_nova_praise.py`, `tests/test_nova_praise_pulse.py`, and `tests/test_flow_delivery_nova_praise_bluestacks.py`. Retained native records include confirmed one-pulse/cooldown sessions and unresolved/blocked attempts; they do not authorize a new production enablement.
- Scope: `tasks/nova_praise.py`; `tasks/nova_praise_pulse.py`; `scripts/flow_delivery_nova_praise_bluestacks.py`; `tasks/gameplay_flow_contracts/NOVA-PRAISE-SUPERVISED-ONE-FREE-PULSE.json`; `tests/test_flow_delivery_nova_praise_bluestacks.py`
- Changes: (1) Make one canonical controller own Home→Research Lab→Nova→one free Praise→bounded Home return. (2) Require current Nova identity, free control, one attempt, and immediate-before target binding; after one dispatch require attempt decrement and active 300-second cooldown (or equivalent positive successor) before completion. (3) Persist occurrence/attempt/cooldown facts through canonical `BotStateManager` SQLite, stop on Personal Might identity, paid/unknown control, overlay, unchanged/unresolved successor, or noncanonical Home, and release ownership rather than holding a long wait. (4) Retire/hard-disable duplicate runtime and flow-delivery callers, install the static runner binding in the same atomic code PR, and keep Personal Might separate.
- Non-goals: No Personal Might Praise, Daily Claim/attribution, repeated pulse in one cooldown, paid/10x action, broad Research Lab rewrite, or scheduler/production enablement.
- Acceptance: (1) Exactly one free Nova Praise is authorized per eligible pulse. (2) Positive attempt decrement and 300-second cooldown successor are required; an unresolved or unchanged result blocks retry. (3) Canonical Home return is bounded and positively recognized. (4) Personal Might target, paid/unknown controls, stale overlays, and duplicate callers cannot satisfy Nova completion. (5) Static runner binding lands with the code PR and its canonical SQLite row remains disabled until REC-O10 acceptance; no separate mutable registration or evidence-dependent registration gate exists.
- Verification: Prescribe existing `python -m unittest tests.test_nova_praise tests.test_nova_praise_pulse tests.test_flow_delivery_nova_praise_bluestacks`; no native run is performed by this planning author.
- Native gate: `REC-O10-D11` (current source, one free dispatch, attempt/cooldown successor, terminal Home, Personal Might separation, and prohibited-input audit). Disabled until prerequisites and explicit operator authorization; historical Nova records remain classified evidence.
- Integration owner: Main owns Nova static runner/ledger cutover. The PR must update `scripts/pnsctl.py`, `automation_service/registry.py`, and `automation_service/state.py`, seed `FlowSpec(default_enabled=false)`, remove/hard-disable duplicate live bindings in `tasks/nova_praise_runtime.py`, `scripts/nova_praise_bluestacks.py`, and `scripts/flow_delivery_nova_praise_bluestacks.py`, and leave Personal Might callers untouched except for explicit identity guards.
- PR boundary: One focused Nova pulse migration PR, title `Canonicalize one-free Nova Praise pulse`, branch `recovery/rec-d11`; current native evidence is separate REC-O10 work.
- Rollback: Disable Nova dispatch while preserving attempt/cooldown ledger and historical records; never substitute Personal Might or restore duplicate pulse callers.
- Runtime authorization: none for coding; zero live/native input. Later supervised validation requires explicit one-pulse budget and authorization.
- Completion: Code PR completion means the canonical controller, canonical SQLite attempt/cooldown state, retired duplicate callers, static runner binding, and focused proof are complete. The separate REC-O10 record later accepts current native Nova evidence; it remains distinct from Daily and Personal Might.

### REC-D12 — Orchestrate a useful Daily routine with observed milestone acceptance
- Task ID: `REC-D12`
- Type: Story
- Priority: P1
- Status: WAITING_DEPENDENCIES
- Objective: Compose a bounded useful Daily routine through the actual `automation_service` scheduler/service/registry and `BotStateManager`, interleaving independently due selected route work with recruitment while keeping ordinary aggregate Claim and milestone chest separate. Each scheduler pulse may make at most one persisted scheduler occurrence claim (not a gameplay Claim) and one active flow selection; a 600-second wait is persisted as a next-due time and releases ownership. The composition can reach a candidate observed 30 Daily points only from independently observed completed-row/result increments; the 30-point goal/threshold additionally requires a separately observed ready chest bound to the same reset/threshold. Supply route acceptance or collection effect is not Daily point credit. It must stop safely on unknown/unresolved state and must not claim full 150-point completion or unsupported rows.
- Dependencies: `REC-F07`, `REC-F14`, `REC-F15`, `REC-D03`, `REC-D04`, `REC-D05`, `REC-D06`, `REC-D07`, `REC-D08`, `REC-D09`, `REC-D10`
- Related work: `DQ-COVERAGE-MATRIX` — retain one-owner catalog/support-flow separation and all dormant flags; `MVP-QUEST-TO-CLAIM` — retain bounded historical Daily/Claim facts, not a full-routine acceptance; `RUNTIME-DECLARATIVE-VERIFIED-FLOW-COMPOSITION` — retain its blocked composition lessons and no-second-framework constraint; `RUNTIME-DECLARATIVE-VERIFIED-FLOW-COMPOSITION-FINAL-READINESS` — retain its historical FAIL/readiness boundary. Route-specific tickets above supersede their old orchestration assumptions; no route outside this dependency set is silently included.
- Evidence: `automation_service/service.py` (`AutomationService` composition root and pulse); `automation_service/scheduler.py` (`_CanonicalPulseCoordinator`/`UtcPulseCoordinator`); `automation_service/registry.py` (static `FlowSpec` runner bindings); `automation_service/state.py` (`BotStateManager` sole mutable authority); `tests/test_automation_service_scheduler_canonical.py`, `tests/test_automation_service_canonical_authority.py`, and `tests/test_automation_service_operations.py`. Legacy `tasks/flow_conductor.py` and `tasks/flow_scenario_attempts.py` are retirement-callsite inputs only, never runtime decision/state authorities; no conductor, queue, execution-matrix, agent, or evidence runtime decision is part of this ticket.
- Scope: `automation_service/service.py`; `automation_service/scheduler.py`; `automation_service/registry.py`; `automation_service/state.py`; `tests/test_automation_service_scheduler_canonical.py`
- Changes: (1) Add the bounded routine as scheduler/service composition over the existing canonical components: each pulse atomically selects one independently due enabled flow occurrence through `BotStateManager`, attaches one active flow/session, executes only selected route records with accepted code prerequisites, then terminalizes/defer/releases before the next pulse. (2) Keep ordinary aggregate Claim and milestone chest as separate route owners; neither follows from objective completion. (3) Interleave one eligible recruitment pass without a long-lived Daily runtime owner; a Basic 600-second cooldown releases lease/flow ownership and is revisited only when independently due. (4) Account points only from observed current increments bound to independently observed completed-row/result observations; require candidate `>=30` only when those increments are real and a same-reset ready chest is separately observed, and never derive points from row count, six×5, or full 150. Supply route acceptance or collection effect alone never credits Daily points. (5) On unknown identity/cost/overlay, unresolved result, stale reset, lease loss, disabled-native route, crash/restart, or reset boundary, stop/defer without unsafe retry while preserving later-ready work in canonical SQLite. (6) Static runner bindings for selected routes land in the same atomic code PR with `default_enabled=false`; no conductor, queue, execution matrix, agent, evidence, or second scheduler makes a runtime decision.
- Non-goals: No all-31-row sweep, full 150-point claim, gathering/shops/Lair subset, Nova pulse, global O10 dependency, conductor/queue/task-state runtime ownership, execution-matrix or evidence-based dispatch, agent orchestration, new orchestration framework, route implementation hidden inside the routine, or inferred points/completion.
- Acceptance: (1) Canonical scheduler/service composition selects only explicitly eligible independently due route records, claims at most one SQLite scheduler occurrence (not a gameplay Claim) and runs at most one active flow per pulse, and interleaves recruitment without duplicate dispatch or shared cooldown corruption. (2) A 600-second wait releases lease/flow ownership; no long overarching Daily runtime owner survives across pulses. (3) Ordinary aggregate Claim and milestone chest are distinct transitions and remain independently durable in canonical SQLite; neither is triggered by objective completion alone. (4) A candidate routine records actual positive increments from independently observed completed rows/results totaling at least 30; the 30-point goal/threshold is valid only when those increments are real and a separately observed ready chest is bound to the same reset/threshold. Supply route acceptance or collection effect is not automatic Daily point credit; six×5 and full 150 are never assumed. (5) The point-threshold native gate additionally requires observed current points from those completed-row/result increments and a separately observed ready chest bound to the same reset/threshold; points alone cannot authorize the chest. (6) Unknown, unresolved, stale, disabled-native, lease-loss, crash/restart, and reset-boundary cases stop/defer without unsafe retry and preserve later-ready work. (7) The routine's code PR can complete offline with static runner bindings and disabled SQLite rows; no conductor/queue/execution-matrix/agent/evidence runtime decision or global O10 prerequisite is required.
- Verification: Prescribe existing `python -m unittest tests.test_automation_service_scheduler_canonical tests.test_automation_service_canonical_authority tests.test_automation_service_operations`; focused temporary fake/replay SQLite tests must cover one-claim/one-active-flow pulse bounds, independently due selection, 600-second ownership release, crash/restart durability, two ready-batch Claim occurrences, and chest-state-versus-points-only negatives. Commands are prescribed, not executed; no native state/input.
- Native gate: Selected route-specific records only: `REC-O10-D03`, `REC-O10-D04`, `REC-O10-D05`, `REC-O10-D06`, `REC-O10-D07A`, `REC-O10-D07B`, `REC-O10-D08`, `REC-O10-D09`, and `REC-O10-D10`. This is a selected-route acceptance ledger, not a dependency on all of REC-O10 or a global O10 gate; no gathering, shop, Lair, or Nova record is required for this candidate routine. The point-threshold gate separately requires actual current-reset points from independently observed completed-row/result increments plus an independently observed same-reset ready chest; Supply free-exhaustion acceptance or collection effect alone never credits Daily points. The composed routine remains disabled through its canonical SQLite rows until each selected route record is accepted in the same deployed composition.
- Integration owner: Main owns the actual `automation_service` scheduler/service/registry composition and `BotStateManager` state. The PR must include exact static runner bindings in `automation_service/registry.py` (`default_enabled=false` only initializes missing SQLite rows), canonical pulse wiring in `automation_service/service.py`/`automation_service/scheduler.py`, one `scripts/pnsctl.py` pulse entrypoint, and the exact selected-route caller cutovers enumerated by D03–D10; mutable state never comes from a queue, `tasks/daily_quest_state.py`, execution matrix, evidence, agent, or conductor. Old `tasks/flow_conductor.py`/`tasks/flow_scenario_attempts.py` are retirement-callsite targets only, and any pnsctl legacy imports are removed/hard-disabled atomically; no separate registration or “wire later” path remains.
- PR boundary: One focused scheduler composition PR, title `Compose bounded 30-point Daily scheduler routine`, branch `recovery/rec-d12`; route code and REC-O10 native records remain separate PRs/operational records.
- Rollback: Disable the composed routine/global gate while retaining per-route results, later-ready batches, and milestone state; never restore a legacy scheduler or full-150 assumption.
- Runtime authorization: none for coding; zero transport, zero native observation, no live scheduler or persistent production enablement. Later operator work requires explicit selected-route budgets and authorization.
- Completion: Code PR completion means the actual canonical scheduler/service/registry composition, one-claim/one-active-flow pulse bound, 600-second ownership release, independent due selection, canonical SQLite persistence, static disabled runner bindings, separate Claim/chest ownership, completed-row/result-bound observed-point proof with the same-reset ready-chest condition, and retired conductor callsites are complete. Supply route acceptance or collection effect alone never credits the 30-point goal. Separate selected route REC-O10 records and later scheduler/native promotion remain required; code merge does not imply native proof, production enablement, R3, R5, or R6.

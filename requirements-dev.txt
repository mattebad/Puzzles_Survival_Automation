# Developer/offline checks support Python 3.11 and newer.
# These are not production dependencies. The checked-in replay and vision tests
# use deterministic fixtures only; pytesseract is imported but no Tesseract
# executable or device runtime is required.
numpy==2.4.6
opencv-python-headless==4.14.0.94
Pillow==12.3.0
pytesseract==0.3.13
packaging==26.3
jsonschema==4.26.0

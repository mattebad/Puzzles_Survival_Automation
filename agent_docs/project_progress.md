# Project Progress

## Current post-merge state

- Repository entry is `main@c0235490f32dedba2c794ce702509052940caf52`; PR #4 merged non-force into `main` at `25f5de6b153afb6b75907b29e91fde5a1d04e122`.
- Campaign r2 remains `blocked_evidence_required` after `LOCALIZATION_NOT_RECOGNIZED` before input; its terminal evidence is retained and no identical retry is authorized.
- `HOME-ATLAS-LOCALIZATION-RESTORATION` is the next inactive task and awaits explicit activation. No flow is active and no runtime ownership is held.
- Production registration remains `NOT_REGISTERED`, scheduler remains disabled, and Nano Material Production maintenance precedes Nanoweapon.
- The pre-existing user modification to `Stop-PnS-OMP.ps1` is preserved and excluded from this reconciliation.

## Goal

Complete `TROOP-TRAINING-END-TO-END-CONSOLIDATION` as the authoritative,
user-configurable four-type Troop Training contract while preserving unrelated work and
superseding `TROOP-TRAINING-VERIFIED-NAVIGATION-CONVERGENCE`. This goal is complete.

## Overall Progress

The production path now enters one canonical Home Atlas facility and uses freshly recognized top
tabs for Fighter, Shooter, Rider, and Vehicle. The named profile remains exact and overrideable:
Fighter T8/current numeric maximum/continuous/boxes allowed; Vehicle T1/current numeric
maximum/continuous/boxes allowed; Shooter T8/fixed 250/once_daily/boxes prohibited; Rider
T1/fixed 250/once_daily/boxes prohibited. The controller proves current numeric maximum before
Train, rejects Train Now/premium/speedup and prohibited resource boxes, persists once-daily state,
and fail-closes continuous scheduling until a completion is positively reconciled.

The one bounded live canary completed with exact active queues Fighter T8 x1000, Shooter T8 x250,
Rider T1 x250, and Vehicle T1 x1000. Current numeric maximum 1000 was positively resolved for
Fighter and Vehicle. No resource boxes were used and no resource/inventory delta was introduced
by box continuation. A terminal native Home frame was verified after safe recovery and singleton
runtime ownership was released. Troop Training remains not registered and scheduler-ineligible.

Prior Ruins chest-coverage evidence remains preserved: the 2026-08-13 session audited all 12
identities, claimed five eligible chests exactly once, reconciled medals `14462 → 16649`
(`+2187`), and returned to canonical Home without combat or spending.

## Verification State

- Latest checked-in focused receipt: 27 tests passed at
  `.local-orchestrator/validation-receipts/TROOP-TRAINING-END-TO-END-CONSOLIDATION/focused_validation/focused_tests-20260813T234249462361Z.json`.
- Latest checked-in shared-navigation receipt: 45 tests passed at
  `.local-orchestrator/validation-receipts/TROOP-TRAINING-END-TO-END-CONSOLIDATION/navigation_validation/shared_navigation-20260813T234441653642Z.json`.
- Independent tester verification completed: package 38 OK; focused and shared-navigation
  profiles passed; post-repair targeted checks passed, including the fake-runtime claim schema
  (`consequential=False`) and adversarial duplicate-Train rejection. The tester independently
  rechecked the repaired Sol criteria and returned PASS.
- Exactly one coherent Sol integration gate ran after the tester package. It found three verifier
  mismatches; Luna completed the final repair turn and the tester verified those repairs. No second
  Sol gate is pending or permitted.
- Historical `py_compile` and Troop-focused `git diff --check` passed; the canonical production path and verifier remain unchanged.
- Post-merge reconciliation validation passed governance 19/19, authority consistency 36/36, both governance/flow-authority CLIs, deterministic 32-task backlog-index generation, and `git diff --check`.
- Token-context hygiene ran 20 tests: 17 passed; three unchanged baseline incompatibilities remain—the obsolete ready-flow fixture, Ultimate's pre-existing completed-versus-blocked assertion conflict, and the pre-existing `active_runtime` expectation mismatch.
- No live emulator input, observation, recovery, or Train action was performed for this reconciliation. Prior end-to-end live evidence remains historical and was not rerun.
- Full repository discovery was not run (manual-only).

## Next Milestone

Exact next action: explicitly activate the new atomic `HOME-ATLAS-LOCALIZATION-RESTORATION` task. It remains inactive until activation; it is not a Campaign r2 continuation or retry.

Campaign r2 remains `blocked_evidence_required`, with no active flow/runtime ownership and no registration or scheduler enablement. The preserved `Stop-PnS-OMP.ps1` user modification remains untouched.
